<view class="page">
  <view wx:if="{{loading}}" class="state-wrap">
    <view class="loading"></view>
    <text class="state-text">加载中…</text>
  </view>

  <view wx:else class="shell">
    <page-header title="完善信息"></page-header>

    <view class="hero">
      <text class="hero-title">完善信息</text>
      <view class="hero-underline"></view>
      <text class="hero-subtitle">首次登录需要绑定以下信息</text>
    </view>

    <view class="form">
      <view class="section">
        <text class="section-title">基础资料</text>

        <view class="field">
          <text class="field-label required">姓名</text>
          <view class="field-control">
            <input
              class="input"
              placeholder="请输入真实姓名"
              placeholder-class="input-placeholder"
              value="{{formData.name}}"
              bindinput="onNameInput"
            />
          </view>
        </view>

        <view class="field">
          <text class="field-label required">性别</text>
          <picker
            mode="selector"
            range="{{sexOptions}}"
            range-key="label"
            value="{{sexIndex}}"
            bindchange="onSexChange"
          >
            <view class="picker-field">
              <text class="picker-text">{{sexOptions[sexIndex].label || '请选择性别'}}</text>
              <text class="picker-arrow">›</text>
            </view>
          </picker>
        </view>

        <view class="field">
          <text class="field-label required">年龄</text>
          <view class="field-control">
            <input
              class="input"
              type="digit"
              maxlength="3"
              placeholder="请输入年龄"
              placeholder-class="input-placeholder"
              value="{{formData.age}}"
              bindinput="onAgeInput"
            />
          </view>
        </view>

        <view class="field">
          <text class="field-label required">职业</text>
          <view class="field-control">
            <input
              class="input"
              placeholder="请输入职业"
              placeholder-class="input-placeholder"
              value="{{formData.occupation}}"
              bindinput="onOccupationInput"
            />
          </view>
        </view>

        <view class="field">
          <text class="field-label required">行业</text>
          <view class="field-control">
            <input
              class="input"
              placeholder="请输入所在行业"
              placeholder-class="input-placeholder"
              value="{{formData.industry}}"
              bindinput="onIndustryInput"
            />
          </view>
        </view>
      </view>

      <view class="section">
        <text class="section-title">联系方式</text>

        <view class="field">
          <text class="field-label required">手机号</text>
          <view class="field-control">
            <input
              class="input {{phoneReadonly ? 'input-readonly' : ''}}"
              type="digit"
              maxlength="11"
              placeholder="请输入11位手机号"
              placeholder-class="input-placeholder"
              value="{{formData.phone}}"
              disabled="{{phoneReadonly}}"
              bindinput="onPhoneInput"
            />
          </view>
          <text class="field-tip" wx:if="{{phoneReadonly}}">已使用微信授权手机号，首次绑定时不可修改</text>
        </view>

        <view class="field">
          <text class="field-label required">邮箱</text>
          <view class="field-control">
            <input
              class="input"
              placeholder="请输入邮箱"
              placeholder-class="input-placeholder"
              value="{{formData.email}}"
              bindinput="onEmailInput"
            />
          </view>
        </view>
      </view>

      <view wx:if="{{error}}" class="error-box">
        <text class="error-text">{{error}}</text>
      </view>
    </view>
  </view>

  <view wx:if="{{!loading}}" class="bottom-bar">
    <button class="btn-submit" disabled="{{submitting}}" bindtap="submit">
      <text class="btn-text">{{submitting ? '提交中…' : '提交'}}</text>
      <text class="btn-arrow">→</text>
    </button>
  </view>
</view>
