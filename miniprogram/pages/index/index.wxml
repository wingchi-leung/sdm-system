<view class="page">
  <view class="hero">
    <view class="hero-top">
      <text class="hero-eyebrow">{{todayLabel}}</text>
      <view class="hero-action" bindtap="goCommunityNotifications">
        <text class="hero-action-mark">•</text>
        <view wx:if="{{communityUnreadCount > 0}}" class="hero-action-badge">
          <text class="hero-action-badge-text">{{communityUnreadCount > 99 ? '99+' : communityUnreadCount}}</text>
        </view>
      </view>
    </view>

    <view class="hero-copy">
      <text class="hero-title">探索未知，</text>
      <text class="hero-title">超越所见。</text>
      <text class="hero-subtitle">Explore the Unexplored.</text>
    </view>

    <view class="hero-rule"></view>

    <text class="hero-date">{{todayDateShort}} {{weekdayLabelEn}}</text>
  </view>

  <view wx:if="{{loading}}" class="state-wrap">
    <view class="loading"></view>
    <text class="state-text">加载中...</text>
  </view>

  <view wx:elif="{{error}}" class="state-wrap error">
    <view class="state-icon">!</view>
    <text class="state-title">加载失败</text>
    <text class="state-text">{{error}}</text>
    <button class="btn-retry" bindtap="onRefresh">重试</button>
  </view>

  <view wx:elif="{{visibleActivities.length === 0}}" class="state-wrap empty">
    <image class="empty-image" src="/assets/icons/noactivity.png" mode="aspectFit" />
    <text class="state-title">暂无活动</text>
    <text class="state-text">稍后刷新，看看有没有新发布的活动。</text>
    <button class="btn-text" bindtap="onRefresh">刷新</button>
  </view>

  <view wx:else class="list-shell">
    <view class="section-header">
      <text class="section-title">即将开始</text>
    </view>

    <view class="activity-list">
      <view
        class="activity-card"
        wx:for="{{visibleActivities}}"
        wx:key="id"
        data-activity="{{item}}"
        bindtap="goDetail"
      >
        <view class="activity-cover">
          <image
            wx:if="{{item.poster_url}}"
            class="activity-cover-image"
            src="{{item.poster_url}}"
            mode="aspectFill"
            lazy-load
          />
          <view wx:else class="activity-cover-placeholder">
            <text class="activity-cover-placeholder-text">SDM</text>
          </view>
        </view>

        <view class="activity-content">
          <view class="activity-head">
            <text class="activity-title-text">{{item.activity_name}}</text>
            <text class="activity-arrow">→</text>
          </view>

          <text wx:if="{{item.summary_text}}" class="activity-summary">{{item.summary_text}}</text>
          <text wx:else class="activity-summary muted">探索更多活动细节</text>

          <view class="activity-meta">
            <text wx:if="{{item.date_label}}" class="activity-meta-text">{{item.date_label}}</text>
            <text wx:if="{{item.weekday_label}}" class="activity-meta-text">{{item.weekday_label}}</text>
            <text wx:if="{{item.time_range_display}}" class="activity-meta-text">{{item.time_range_display}}</text>
            <text class="activity-meta-text">{{item.location_display}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class="footer-space"></view>
</view>
