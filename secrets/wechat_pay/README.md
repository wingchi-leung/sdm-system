# 微信支付证书目录

将以下证书文件放入当前目录，供 Docker 后端容器挂载使用：

- `apiclient_key.pem`：商户私钥
- `wx_public_key.pem`：微信支付公钥（使用公钥模式时需要）

说明：

- 该目录用于 Docker 部署和跨机器迁移，避免在容器内引用宿主机的绝对路径。
- 私钥和公钥文件已被仓库 `.gitignore` 忽略，不会提交到版本库。
- 迁移到新机器时，请连同本目录下的证书文件一起复制。
