# 后端代码审查报告

> 审查日期：2026-03-21
> 审查范围：backend/ 目录全部代码
> 审查维度：安全性、高性能、高并发、事务一致性
> 最后更新：2026-03-21（第一阶段修复完成）

---

## 一、审查概要

| 类别 | 高危 | 中危 | 低危 | 总计 | 已修复 |
|------|------|------|------|------|--------|
| 安全性 | 6 | 4 | 1 | 11 | 2 |
| 高并发/性能 | 4 | 4 | 1 | 9 | 2 |
| 事务一致性 | 3 | 2 | 0 | 5 | 3 |
| 输入验证 | 0 | 4 | 0 | 4 | 0 |
| **总计** | **13** | **14** | **2** | **29** | **7** |

---

## 二、安全性问题

### 2.1 高危问题

#### ✅ SEC-001: JWT Secret 默认值不安全（已修复）
**文件**: `backend/app/core/config.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 移除 JWT_SECRET 默认值，强制要求配置
- 应用启动时检查是否为不安全默认值，拒绝启动
- 添加 `sys.exit(1)` 强制退出保护

---

#### 🔴 SEC-002: CORS 配置过于宽松
**文件**: `backend/app/main.py:19-25`
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 允许所有来源
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
**风险**: 允许任意域名跨域访问，配合 `allow_credentials=True` 可能导致 CSRF 攻击窃取用户凭证。

**修复方案**:
1. 添加环境变量 `CORS_ORIGINS` 配置允许的域名列表
2. 生产环境禁止使用 `*` 配置

---

#### 🔴 SEC-003: 登录限流使用内存存储
**文件**: `backend/app/api/v1/endpoints/auth.py:22`
```python
_login_attempts: dict[str, list[float]] = defaultdict(list)
```
**风险**:
- 服务重启后限流记录丢失
- 无内存上限，可能被恶意 IP 耗尽内存导致 OOM
- 多实例部署时各实例独立限流（若部署多实例需额外处理）

**修复方案**:
1. **优化内存方案**（推荐，适用于单实例部署）:
   - 添加内存上限（如最多存储 10000 个 IP）
   - 定期清理过期记录（每次检查时清理）
   - 添加 IP 黑名单功能
2. **多实例部署替代方案**:
   - Nginx 层限流（推荐）：使用 `limit_req_zone` 和 `limit_req`
   - 数据库限流表：记录 IP 和时间戳，定时清理

---

#### 🔴 SEC-004: 微信 access_token 使用内存缓存
**文件**: `backend/app/api/v1/endpoints/auth.py:25`
```python
_wechat_access_token_cache: dict[str, dict] = {}
```
**风险**: 服务重启后需重新获取 token；多实例部署时每个实例单独请求可能触发微信频率限制。

**修复方案**:
1. **单实例部署**（当前方案可接受）:
   - 添加过期时间检查和自动刷新机制
   - 服务启动时主动获取 token
2. **多实例部署替代方案**:
   - **数据库存储**：将 token 存入数据库配置表，定时任务统一刷新
   - **中控服务**：独立的 token 服务，所有实例从中获取

---

#### ✅ SEC-005: SQL 日志在生产环境开启（已修复）
**文件**: `backend/app/database.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 添加 `DB_ECHO` 环境变量控制 SQL 日志开关
- 默认值设为 `False`，生产环境安全
- 同时配置了数据库连接池参数 (`pool_size=20, max_overflow=10`)

---

#### ✅ SEC-010: 支付回调日志泄露敏感数据（已修复）
**文件**: `backend/app/api/v1/endpoints/payments.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 移除完整回调数据打印
- 仅记录订单号和交易状态，不记录敏感信息

---

#### 🔴 SEC-011: 支付金额缺少上限验证
**文件**: `backend/app/api/v1/endpoints/payments.py:72`
```python
if order_in.actual_fee < activity.suggested_fee:
    raise HTTPException(...)
```
**风险**: 只验证下限，未验证上限。恶意用户可能创建超大金额订单，导致：
- 微信支付单笔限额问题
- 业务风险（活动费用异常）
- 潜在的资金安全问题

**修复方案**:
```python
MAX_PAYMENT_AMOUNT = 1000000  # 1万元上限
if order_in.actual_fee > MAX_PAYMENT_AMOUNT:
    raise HTTPException(status_code=400, detail="支付金额超出上限")
```

---

### 2.2 中危问题

#### 🟡 SEC-006: 缺少请求体大小限制
**风险**: 未配置请求体大小限制，可能被大文件上传攻击耗尽服务器资源。

**修复方案**:
```python
app = FastAPI(...)
app.add_middleware(RequestSizeLimitMiddleware, max_body_size=10 * 1024 * 1024)  # 10MB
```

---

#### 🟡 SEC-007: 敏感信息可能出现在日志中
**文件**: `backend/app/crud/crud_activity.py:139`
```python
print(f"[DEBUG] 更新活动 {activity_id}, 收到数据: {update_data}")
```
**风险**: 调试打印可能泄露用户敏感信息。

**修复方案**: 移除所有调试打印，使用结构化日志框架。

---

#### 🟡 SEC-008: 用户输入直接用于模糊搜索
**文件**: `backend/app/crud/crud_user.py:228-232`
```python
keyword_pattern = f"%{keyword}%"
query = query.filter(User.name.ilike(keyword_pattern))
```
**风险**: 虽然使用 ORM 参数化查询防止了 SQL 注入，但缺少关键字过滤，恶意用户可使用 `%` 通配符导致全表扫描。

**修复方案**: 过滤用户输入中的特殊字符 `%` 和 `_`。

---

#### ✅ SEC-012: 支付回调缺少防重放攻击机制（已修复）
**文件**: `backend/app/api/v1/endpoints/payments.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 使用数据库行锁 `with_for_update()` 防止并发
- 添加幂等检查：订单已成功时直接返回
- 整个处理过程使用事务包裹

---

#### 🟡 SEC-013: 订单创建与微信下单非原子操作
**文件**: `backend/app/api/v1/endpoints/payments.py:87-117`

**问题**: 先调用微信下单，再保存订单到数据库：
```python
# 1. 调用微信统一下单
result = pay_service.create_jsapi_order(...)

# 2. 保存订单到数据库（如果这里失败？）
db_order = crud_payment.create_payment_order(...)
```

**风险**: 如果数据库保存失败，微信侧已创建预支付订单，导致数据不一致。

**修复方案**:
1. 先创建数据库订单（状态为"创建中"）
2. 调用微信下单
3. 更新订单状态和 prepay_id
4. 使用事务确保一致性

---

### 2.3 低危问题

#### 🟢 SEC-009: 密码哈希缺少工作因子配置
**文件**: `backend/app/core/security.py:6`
```python
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
```
**建议**: 明确配置 bcrypt 的 rounds 参数（建议 12 或更高）。

---

## 三、高性能与高并发问题

### 3.1 严重问题

#### 🔴 PERF-001: 签到存在竞态条件 (Race Condition)
**文件**: `backend/app/crud/crud_checkin.py:67-94`

**问题代码**:
```python
def create_checkin(db: Session, checkin: CheckInCreate, tenant_id: int):
    # 检查是否已签到
    existing_checkin = check_already_checkin(db, ...)
    if existing_checkin:
        raise HTTPException(...)

    # 创建签到 - 并发问题！
    db_checkin = CheckInRecord(**checkin_data)
    db.add(db_checkin)
    db.commit()
```

**场景**: 用户快速点击签到按钮，两个请求同时通过"是否已签到"检查，导致重复签到。

**修复方案**:
1. 数据库层：添加唯一约束 `UNIQUE(activity_id, identity_number, tenant_id)`
2. 应用层：捕获唯一约束异常（IntegrityError），返回友好提示

---

#### 🔴 PERF-002: 报名存在竞态条件
**文件**: `backend/app/crud/crud_participant.py:84-111`

**问题**: 与签到问题相同，检查与创建之间存在时间窗口。

**修复方案**: 同 PERF-001。

---

#### 🔴 PERF-003: 用户注册存在竞态条件
**文件**: `backend/app/crud/crud_user.py:98-132`

**问题**: 检查手机号/邮箱是否存在与创建用户之间存在时间窗口。

**修复方案**:
1. 添加数据库唯一约束 `UNIQUE(phone, tenant_id)` 和 `UNIQUE(email, tenant_id)`
2. 捕获唯一约束异常返回友好提示

---

#### ✅ PERF-004: 支付回调存在竞态条件（已修复）
**文件**: `backend/app/api/v1/endpoints/payments.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 使用数据库行锁 `with_for_update()` 获取订单
- 添加幂等检查：订单已成功时直接返回
- 整个处理过程使用事务保护，确保原子性

---

### 3.2 中等问题

#### 🟡 PERF-005: 缺少数据库索引
**文件**: `backend/app/schemas.py`

**问题字段**:
| 表 | 字段 | 建议 |
|---|---|---|
| activity_participants | activity_id | 添加索引 |
| activity_participants | identity_number | 添加索引 |
| checkin_records | activity_id | 添加索引 |
| checkin_records | identity_number | 添加索引 |

**影响**: 签到和报名查询性能随数据量增长急剧下降。

---

#### ✅ PERF-006: 缺少数据库连接池大小配置（已修复）
**文件**: `backend/app/database.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
```python
engine = create_engine(
    SQLALCHEMY_DATABASE_URL,
    pool_size=20,        # 常驻连接数
    max_overflow=10,     # 最大溢出连接数
    pool_pre_ping=True,
    pool_recycle=3600,
    echo=settings.DB_ECHO,
)
```

---

#### 🟡 PERF-007: N+1 查询问题
**文件**: `backend/app/crud/crud_activity.py:43-56`

**问题**: 创建活动时逐个插入参与者：
```python
for participant in participants:
    db.add(ActivityParticipant(**participant_dict))
```

**修复方案**: 使用批量插入：
```python
db.bulk_insert_mappings(ActivityParticipant, participant_list)
```

---

#### 🟡 PERF-008: 缺少应用层缓存

**问题**: 部分高频访问数据每次都查询数据库，如租户信息、活动类型、用户权限范围。

**修复方案**（无需 Redis 的替代方案）:

1. **应用内存缓存**（适用于单实例部署）:
   ```python
   # 简单的内存缓存实现
   from functools import lru_cache
   from datetime import datetime, timedelta

   _cache: dict = {}
   _cache_expire: dict = {}

   def get_cached(key: str, ttl_seconds: int, loader):
       """通用缓存获取函数"""
       now = datetime.now()
       if key in _cache and _cache_expire.get(key, now) > now:
           return _cache[key]
       value = loader()
       _cache[key] = value
       _cache_expire[key] = now + timedelta(seconds=ttl_seconds)
       return value
   ```

2. **启动时预加载热点数据**:
   - 应用启动时加载租户配置、活动类型等不常变化的数据
   - 提供手动刷新接口

3. **数据库查询优化**:
   - 确保相关字段有索引
   - 使用 JOIN 减少查询次数
   - 对大表使用覆盖索引

**建议缓存的数据**:
| 数据 | 缓存策略 | 过期时间 |
|---|---|---|
| 租户信息 | 应用内存缓存 | 10 分钟或手动刷新 |
| 活动类型列表 | 启动预加载 | 手动刷新 |
| 用户权限范围 | 应用内存缓存 | 5 分钟 |
| 微信 access_token | 应用内存缓存 | 1.5 小时 |

---

### 3.3 低危问题

#### 🟢 PERF-009: 分页查询 COUNT 性能
**文件**: `backend/app/crud/crud_activity.py:88`

**问题**: 大数据量下 `query.count()` 性能较差。

**建议**: 考虑使用近似计数或缓存总数。

---

## 四、事务一致性问题

### 4.1 严重问题

#### ✅ TX-001: 删除活动未使用事务保护（已确认正确）
**文件**: `backend/app/crud/crud_activity.py:166-190`
**状态**: 经检查，代码已正确实现 try-except-rollback 模式

**现有实现**:
```python
def delete_activity(db: Session, activity_id: int, tenant_id: int):
    try:
        db.query(ActivityParticipant).filter(...).delete()
        db.query(CheckInRecord).filter(...).delete()
        db.delete(db_activity)
        db.commit()
        return True
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))
```

---

#### ✅ TX-002: 创建活动事务范围不当（已确认正确）
**文件**: `backend/app/crud/crud_activity.py:24-64`
**状态**: 经检查，代码已正确实现 try-except-rollback 模式

**现有实现**:
```python
def create_activity(db: Session, activity: ActivityCreate, tenant_id: int) -> Activity:
    try:
        # 创建活动和参与者...
        db.commit()
        return db_activity
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))
```

---

#### ✅ TX-003: 支付回调处理未使用事务（已修复）
**文件**: `backend/app/api/v1/endpoints/payments.py`
**状态**: 已修复 (2026-03-21)

**修复内容**:
- 使用行锁 `with_for_update()` 获取订单
- 添加幂等检查：订单已成功时直接返回
- 整个处理过程使用事务包裹，确保原子性
- 支付成功时：更新订单状态、创建参与者、更新关联一次性完成
- 失败时自动回滚

**修复后代码**:
```python
# 使用行锁获取订单
order = db.query(PaymentOrder).filter(
    PaymentOrder.order_no == order_no
).with_for_update().first()

# 幂等检查
if order.status == 1:
    return {"code": "SUCCESS", "message": "已处理"}

if trade_state == "SUCCESS":
    try:
        # 原子操作：更新订单 + 创建参与者 + 更新关联
        order.status = 1
        order.transaction_id = transaction_id
        participant = ActivityParticipant(...)
        db.add(participant)
        db.flush()
        order.participant_id = participant.id
        db.commit()
    except Exception as e:
        db.rollback()
        return {"code": "FAIL", "message": "处理失败"}
```

---

### 4.2 中等问题

#### 🟡 TX-004: 权限检查与数据操作非原子
**文件**: `backend/app/api/deps.py:119-140`

**问题**: 权限检查完成后，实际操作前，数据可能被其他请求修改或删除。

**建议**: 在事务中重新验证或使用乐观锁。

---

#### 🟡 TX-005: 创建支付订单未处理微信下单失败回滚
**文件**: `backend/app/api/v1/endpoints/payments.py:87-117`

**问题**: 微信下单成功后，如果数据库保存失败，微信侧已创建预支付订单。

**修复方案**:
1. 先创建数据库订单（状态为"创建中"）
2. 调用微信下单
3. 成功后更新 prepay_id
4. 失败则删除数据库订单或标记为失败

---

## 五、输入验证问题

### 5.1 中等问题

#### 🟡 VAL-001: 手机号验证规则不一致
**文件**: `backend/app/models/user.py`

| 模型 | 验证规则 |
|---|---|
| UserBindInfoRequest.phone | 11 位中国手机号 |
| UserBase.phone | 只验证最大长度 |
| RegisterRequest.phone | 只验证非空 |

**建议**: 统一使用中国手机号验证规则。

---

#### 🟡 VAL-002: 身份证号缺少格式验证
**文件**: `backend/app/models/participant.py:10`

**问题**: `identity_number` 只验证最大长度，无格式验证。

**建议**: 根据证件类型添加相应的格式验证。

---

#### 🟡 VAL-003: 活动类型名称缺少长度验证
**文件**: `backend/app/models/activity.py:23`

**问题**: `activity_type_name` 无长度限制。

**建议**: 添加 `max_length` 限制。

---

#### 🟡 VAL-004: 支付金额缺少上下限验证
**文件**: `backend/app/api/v1/endpoints/payments.py:72`

**问题**: 只验证金额不低于建议费用，未验证：
- 金额上限（可能导致超大金额订单）
- 金额为整数（分单位，应为正整数）

**建议**:
```python
MAX_PAYMENT_AMOUNT = 1000000  # 1万元
if order_in.actual_fee <= 0:
    raise HTTPException(status_code=400, detail="支付金额必须大于0")
if order_in.actual_fee > MAX_PAYMENT_AMOUNT:
    raise HTTPException(status_code=400, detail="支付金额超出上限")
```

---

## 六、微信支付模块专项审查

### 6.1 业务逻辑问题

#### 🟡 PAY-001: 参与者信息在回调中丢失
**文件**: `backend/app/api/v1/endpoints/payments.py:193-204`

**问题**: 创建参与者时，姓名和手机号为空：
```python
participant = crud_payment.create_participant_with_payment(
    participant_name="",  # 空字符串！
    phone="",             # 空字符串！
    ...
)
```

**原因**: 用户在创建订单时填写的信息未存储到订单表，回调时无法获取。

**修复方案**:
1. 在 PaymentOrder 表添加 `participant_name` 和 `phone` 字段
2. 创建订单时保存这些信息
3. 回调时从订单中读取

---

#### 🟡 PAY-002: 缺少订单超时关闭机制
**文件**: `backend/app/crud/crud_payment.py:145-174`

**问题**: 虽然有 `close_expired_orders` 函数，但：
- 没有定时任务调用
- 订单过期后状态不会自动更新
- 可能导致微信侧订单未关闭，用户仍可支付

**修复方案**:
1. 添加定时任务（如每5分钟）执行订单关闭
2. 关闭时同步调用微信订单关闭接口
```python
# 定时任务示例
from apscheduler.schedulers.background import BackgroundScheduler

scheduler = BackgroundScheduler()
scheduler.add_job(close_all_expired_orders, 'interval', minutes=5)
scheduler.start()
```

---

#### 🟡 PAY-003: 支付服务单例初始化异常未处理
**文件**: `backend/app/services/wechat_pay.py:190-195`

**问题**: 如果配置不完整，第一次调用会抛出异常，但全局实例保持 `None`：
```python
def get_wechat_pay_service() -> WeChatPayService:
    global wechat_pay_service
    if wechat_pay_service is None:
        wechat_pay_service = WeChatPayService()  # 可能抛出 ValueError
    return wechat_pay_service
```

**风险**: 后续调用每次都会尝试初始化，可能导致频繁报错。

**建议**:
1. 应用启动时预先初始化并捕获异常
2. 添加健康检查接口

---

### 6.2 微信支付索引建议

| 表 | 字段 | 索引类型 | 说明 |
|---|---|---|---|
| payment_order | order_no | UNIQUE | 已有 |
| payment_order | tenant_id | INDEX | 已有 |
| payment_order | activity_id | INDEX | 已有 |
| payment_order | user_id | INDEX | 已有 |
| payment_order | status | INDEX | 已有 |
| payment_order | expire_at | INDEX | 建议：定时任务查询过期订单 |

---

## 七、修复计划 TODO

### 第一阶段：紧急修复（1-2 天）✅ 已完成

- [x] **SEC-001**: 强制配置 JWT_SECRET，移除默认值
- [x] **SEC-005**: 生产环境关闭 SQL 日志
- [x] **TX-001**: 修复删除活动事务问题（已存在正确实现）
- [x] **TX-002**: 修复创建活动事务问题（已存在正确实现）
- [x] **TX-003**: 修复支付回调事务问题
- [x] **SEC-010**: 移除支付回调敏感日志

### 第二阶段：竞态条件修复（3-5 天）

- [ ] **PERF-001**: 签到竞态条件修复
  - [ ] 添加数据库唯一约束
  - [ ] 修改代码捕获唯一约束异常
- [ ] **PERF-002**: 报名竞态条件修复
  - [ ] 添加数据库唯一约束
  - [ ] 修改代码捕获唯一约束异常
- [ ] **PERF-003**: 用户注册竞态条件修复
  - [ ] 添加数据库唯一约束
  - [ ] 修改代码捕获唯一约束异常
- [ ] **PERF-004**: 支付回调竞态条件修复
  - [x] 添加行锁或乐观锁
  - [x] 添加幂等处理

### 第三阶段：安全加固（3-5 天）

- [ ] **SEC-002**: CORS 配置优化
- [ ] **SEC-003**: 优化登录限流
  - [ ] 添加内存上限
  - [ ] 添加过期清理机制
  - [ ] 可选：Nginx 层限流
- [ ] **SEC-004**: 优化微信 access_token 缓存
  - [ ] 添加自动刷新机制
  - [ ] 多实例场景可选：数据库存储
- [ ] **SEC-006**: 添加请求体大小限制
- [ ] **SEC-007**: 清理调试代码
- [ ] **SEC-008**: 过滤搜索关键字特殊字符
- [ ] **SEC-011**: 添加支付金额上限验证
- [ ] **SEC-012**: 支付回调添加幂等处理

### 第四阶段：性能优化（5-7 天）

- [ ] **PERF-005**: 添加数据库索引
- [ ] **PERF-006**: 配置数据库连接池
- [ ] **PERF-007**: 优化批量插入
- [ ] **PERF-008**: 添加应用内存缓存
  - [ ] 实现通用缓存函数
  - [ ] 缓存租户信息、活动类型等
  - [ ] 启动时预加载热点数据

### 第五阶段：代码质量（2-3 天）

- [ ] **VAL-001**: 统一手机号验证规则
- [ ] **VAL-002**: 添加身份证号格式验证
- [ ] **VAL-004**: 添加支付金额验证
- [ ] **SEC-009**: 配置 bcrypt 工作因子
- [ ] 清理所有调试打印语句
- [ ] 添加枚举类替代魔法数字

### 第六阶段：微信支付完善（3-5 天）

- [ ] **PAY-001**: 修复参与者信息丢失问题
  - [ ] PaymentOrder 表添加 participant_name 和 phone 字段
  - [ ] 创建订单时保存参与者信息
- [ ] **PAY-002**: 添加订单超时关闭机制
  - [ ] 添加定时任务
  - [ ] 同步调用微信订单关闭接口
- [ ] **PAY-003**: 支付服务启动时初始化检查
- [ ] **TX-005**: 创建支付订单添加回滚机制

---

## 八、数据库索引与约束变更清单

### 新增索引

```sql
-- 活动参与人表
CREATE INDEX idx_participant_activity_id ON activity_participants(activity_id);
CREATE INDEX idx_participant_identity ON activity_participants(identity_number, tenant_id);

-- 签到记录表
CREATE INDEX idx_checkin_activity_id ON checkin_records(activity_id);
CREATE INDEX idx_checkin_identity ON checkin_records(identity_number, tenant_id);
```

### 新增唯一约束

```sql
-- 防止重复签到
ALTER TABLE checkin_records
ADD UNIQUE INDEX uk_checkin_unique (activity_id, identity_number, tenant_id);

-- 防止重复报名
ALTER TABLE activity_participants
ADD UNIQUE INDEX uk_participant_unique (activity_id, identity_number, tenant_id);

-- 防止重复手机号
ALTER TABLE user
ADD UNIQUE INDEX uk_user_phone (phone, tenant_id);

-- 防止重复邮箱
ALTER TABLE user
ADD UNIQUE INDEX uk_user_email (email, tenant_id);
```

---

## 九、应用内存缓存设计

### 缓存管理器实现

```python
# backend/app/utils/cache.py
from datetime import datetime, timedelta
from typing import Any, Callable, Optional
import threading

class SimpleCache:
    """简单的线程安全内存缓存"""

    def __init__(self):
        self._cache: dict[str, Any] = {}
        self._expire: dict[str, datetime] = {}
        self._lock = threading.Lock()

    def get(self, key: str) -> Optional[Any]:
        """获取缓存值，过期返回 None"""
        with self._lock:
            if key not in self._cache:
                return None
            if datetime.now() > self._expire.get(key, datetime.max):
                del self._cache[key]
                del self._expire[key]
                return None
            return self._cache[key]

    def set(self, key: str, value: Any, ttl_seconds: int):
        """设置缓存值"""
        with self._lock:
            self._cache[key] = value
            self._expire[key] = datetime.now() + timedelta(seconds=ttl_seconds)

    def delete(self, key: str):
        """删除缓存"""
        with self._lock:
            self._cache.pop(key, None)
            self._expire.pop(key, None)

    def clear(self):
        """清空所有缓存"""
        with self._lock:
            self._cache.clear()
            self._expire.clear()

# 全局缓存实例
cache = SimpleCache()

def cached(key: str, ttl_seconds: int, loader: Callable[[], Any]) -> Any:
    """缓存获取辅助函数"""
    value = cache.get(key)
    if value is not None:
        return value
    value = loader()
    cache.set(key, value, ttl_seconds)
    return value
```

### 使用示例

```python
# 缓存租户信息
def get_tenant_cached(db: Session, tenant_id: int) -> Tenant:
    return cached(
        f"tenant:{tenant_id}",
        ttl_seconds=600,  # 10 分钟
        loader=lambda: db.query(Tenant).filter(Tenant.id == tenant_id).first()
    )

# 缓存用户权限范围
def get_admin_scope_cached(db: Session, admin_id: int, tenant_id: int):
    return cached(
        f"admin_scope:{admin_id}:{tenant_id}",
        ttl_seconds=300,  # 5 分钟
        loader=lambda: crud_admin.get_admin_scope(db, admin_id, tenant_id)
    )
```

### 缓存键规范

| 数据类型 | 键格式 | TTL |
|---------|-------|-----|
| 租户信息 | `tenant:{tenant_id}` | 600s |
| 活动类型列表 | `activity_types:{tenant_id}` | 600s |
| 用户权限范围 | `admin_scope:{admin_id}:{tenant_id}` | 300s |
| 微信 access_token | `wechat_token:{appid}` | 5400s |

### 多实例部署说明

如果未来需要多实例部署，可选择以下方案：

1. **Nginx 负载均衡 + 会话保持**：同一用户请求始终路由到同一实例
2. **数据库缓存表**：将缓存数据存入数据库，适合更新频率低的场景
3. **独立缓存服务**：后续可独立部署缓存服务，无需改动业务代码

---

## 十、环境变量配置清单

建议在 `.env` 中添加以下配置：

```env
# ===== 必需配置 =====
# JWT 密钥（必须配置，不提供默认值）
JWT_SECRET=your-secure-random-string-at-least-32-chars

# ===== 数据库配置 =====
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=your-password
MYSQL_DB=sdm_db
MYSQL_PORT=3306

# ===== 可选配置 =====
# SQL 日志开关（生产环境设为 false）
DB_ECHO=false

# CORS 配置（生产环境配置具体域名）
CORS_ORIGINS=https://your-domain.com,https://admin.your-domain.com

# 数据库连接池（已内置默认值，可选覆盖）
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# 请求限制
MAX_REQUEST_BODY_SIZE=10485760  # 10MB

# 密码哈希
BCRYPT_ROUNDS=12

# 登录限流配置
LOGIN_RATE_LIMIT_COUNT=10
LOGIN_RATE_LIMIT_WINDOW_SECONDS=60
LOGIN_RATE_LIMIT_MAX_IPS=10000

# ===== 微信配置 =====
WECHAT_APPID=your-appid
WECHAT_SECRET=your-secret

# 微信支付配置
WECHAT_PAY_MCH_ID=your-mch-id
WECHAT_PAY_API_V3_KEY=your-api-v3-key
WECHAT_PAY_SERIAL_NO=your-serial-no
WECHAT_PAY_PRIVATE_KEY_PATH=/path/to/private_key.pem
WECHAT_PAY_NOTIFY_URL=https://your-domain.com/api/v1/payments/notify
```

---

## 十一、总结

本次审查共发现 **29 个问题**，其中：
- **13 个高危问题**：已修复 5 个
- **14 个中危问题**：已修复 2 个
- **2 个低危问题**：可择机优化

### 修复进度

| 阶段 | 状态 | 已修复 | 待修复 |
|------|------|--------|--------|
| 第一阶段：紧急修复 | ✅ 已完成 | 6 | 0 |
| 第二阶段：竞态条件修复 | 🔄 进行中 | 1 | 3 |
| 第三阶段：安全加固 | ⏳ 待开始 | 0 | 8 |
| 第四阶段：性能优化 | 🔄 进行中 | 2 | 2 |
| 第五阶段：代码质量 | ⏳ 待开始 | 0 | 6 |
| 第六阶段：微信支付完善 | ⏳ 待开始 | 0 | 4 |

### 已修复问题清单

| 编号 | 问题 | 修复日期 |
|------|------|----------|
| SEC-001 | JWT Secret 默认值不安全 | 2026-03-21 |
| SEC-005 | SQL 日志在生产环境开启 | 2026-03-21 |
| SEC-010 | 支付回调日志泄露敏感数据 | 2026-03-21 |
| SEC-012 | 支付回调缺少防重放攻击机制 | 2026-03-21 |
| PERF-004 | 支付回调存在竞态条件 | 2026-03-21 |
| PERF-006 | 缺少数据库连接池大小配置 | 2026-03-21 |
| TX-001 | 删除活动未使用事务保护 | 已确认正确 |
| TX-002 | 创建活动事务范围不当 | 已确认正确 |
| TX-003 | 支付回调处理未使用事务 | 2026-03-21 |

### 问题分布

| 模块 | 问题数量 | 关键问题 |
|------|----------|----------|
| 基础架构 | 23 | JWT安全、竞态条件、事务一致性 |
| 微信支付 | 9 | 回调幂等、事务保护、金额验证 |

### 优先级建议

1. **最关键**：竞态条件（PERF-001/002/003），通过数据库唯一约束解决
2. **次关键**：CORS 配置优化（SEC-002）
3. **重要**：支付金额验证（SEC-011/VAL-004），防止业务风险

建议按照修复计划分阶段实施。