# SDM 系统多租户迁移方案

## 一、项目概述

### 1.1 目标

将 SDM 系统从单租户架构改造为多租户 SaaS 架构，实现：
- 多个组织/客户共享同一套系统
- 数据完全隔离，每个租户只能看到自己的数据
- 支持租户独立管理用户、活动、签到记录

### 1.2 当前架构问题

| 问题 | 影响 |
|------|------|
| 数据表无租户标识 | 所有数据全局共享，无法区分归属 |
| JWT 无租户信息 | 无法在请求层面识别租户 |
| 查询无隔离逻辑 | 任何用户可看到所有数据 |
| 管理员全局唯一 | 无法区分不同租户的管理员 |

### 1.3 技术选型

| 决策项 | 方案 | 理由 |
|--------|------|------|
| 隔离策略 | 共享数据库 + tenant_id 字段 | 成本低、维护简单、适合中小规模 SaaS |
| 租户识别 | JWT 携带 tenant_id | 与现有鉴权体系无缝集成 |
| 数据迁移 | 默认租户 ID = 1 | 现有数据归属默认租户 |

---

## 二、数据库迁移

### 2.1 新增租户表

```sql
-- 租户主表
CREATE TABLE IF NOT EXISTS `tenant` (
  `id` int NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '租户名称（如：XX社区）',
  `code` varchar(32) NOT NULL COMMENT '租户编码（用于API标识，如：xx_community）',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态：1-正常 0-禁用 2-欠费',
  `plan` varchar(32) DEFAULT 'basic' COMMENT '套餐：basic/pro/enterprise',
  `max_admins` int DEFAULT 5 COMMENT '最大管理员数',
  `max_activities` int DEFAULT 100 COMMENT '最大活动数',
  `expire_at` datetime DEFAULT NULL COMMENT '服务到期时间',
  `contact_name` varchar(64) DEFAULT NULL COMMENT '联系人',
  `contact_phone` varchar(32) DEFAULT NULL COMMENT '联系电话',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_tenant_code` (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='租户表';

-- 创建默认租户（现有数据归属）
INSERT INTO `tenant` (`id`, `name`, `code`, `status`, `plan`)
VALUES (1, '默认租户', 'default', 1, 'enterprise');
```

### 2.2 现有表添加租户字段

```sql
-- ============================================================
-- 核心业务表添加 tenant_id
-- ============================================================

-- 1. 用户表
ALTER TABLE `user` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_user_tenant_id` (`tenant_id`);

-- 删除原有唯一索引，改为租户内唯一
ALTER TABLE `user` DROP INDEX `uk_user_phone`;
ALTER TABLE `user` ADD UNIQUE KEY `uk_tenant_phone` (`tenant_id`, `phone`);
ALTER TABLE `user` DROP INDEX `uk_user_wx_openid`;
ALTER TABLE `user` ADD UNIQUE KEY `uk_tenant_wx_openid` (`tenant_id`, `wx_openid`);

-- 2. 管理员表
ALTER TABLE `admin_user` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_admin_tenant_id` (`tenant_id`);

-- 删除原有唯一索引，改为租户内唯一
ALTER TABLE `admin_user` DROP INDEX `username`;
ALTER TABLE `admin_user` ADD UNIQUE KEY `uk_tenant_username` (`tenant_id`, `username`);

-- 3. 活动类型表
ALTER TABLE `activity_type` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_activity_type_tenant_id` (`tenant_id`);

ALTER TABLE `activity_type` DROP INDEX `uk_activity_type_name`;
ALTER TABLE `activity_type` ADD UNIQUE KEY `uk_tenant_type_name` (`tenant_id`, `type_name`);

-- 4. 活动表
ALTER TABLE `activity` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_activity_tenant_id` (`tenant_id`);

-- 5. 活动参与人表
ALTER TABLE `activity_participants` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_participants_tenant_id` (`tenant_id`);

-- 6. 签到记录表
ALTER TABLE `checkin_records` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_checkin_tenant_id` (`tenant_id`);

-- 7. 管理员-活动类型授权表
ALTER TABLE `admin_activity_type_role` 
  ADD COLUMN `tenant_id` int NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER `id`,
  ADD INDEX `idx_admin_role_tenant_id` (`tenant_id`);

-- ============================================================
-- 创建复合索引优化查询
-- ============================================================

-- 活动表：租户+状态查询
ALTER TABLE `activity` ADD INDEX `idx_tenant_status` (`tenant_id`, `status`);

-- 签到表：租户+活动查询
ALTER TABLE `checkin_records` ADD INDEX `idx_tenant_activity` (`tenant_id`, `activity_id`);

-- 参与人表：租户+活动查询
ALTER TABLE `activity_participants` ADD INDEX `idx_tenant_activity` (`tenant_id`, `activity_id`);
```

### 2.3 迁移脚本（可执行 SQL 文件）

创建文件：`backend/migrations/add_tenant_fields.sql`

---

## 三、后端代码改造

### 3.1 新增租户模型

**文件：`backend/app/schemas.py`** - 添加租户表模型

```python
# 在文件末尾添加

class Tenant(BaseModel):
    __tablename__ = "tenant"
    name = Column(String(100), nullable=False)
    code = Column(String(32), unique=True, nullable=False, index=True)
    status = Column(Integer, default=1)  # 1-正常 0-禁用 2-欠费
    plan = Column(String(32), default='basic')
    max_admins = Column(Integer, default=5)
    max_activities = Column(Integer, default=100)
    expire_at = Column(DateTime, nullable=True)
    contact_name = Column(String(64), nullable=True)
    contact_phone = Column(String(32), nullable=True)
```

**文件：`backend/app/schemas.py`** - 修改现有模型，添加 tenant_id

```python
# 修改 Activity 类
class Activity(BaseModel):
    __tablename__ = "activity"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    activity_name = Column(String(100))
    # ... 其他字段不变

# 修改 User 类
class User(BaseModel):
    __tablename__ = "user"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    name = Column(String(255))
    # ... 其他字段不变

# 修改 AdminUser 类
class AdminUser(BaseModel):
    __tablename__ = "admin_user"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    username = Column(String(64), nullable=False, index=True)
    # ... 其他字段不变

# 修改 ActivityType 类
class ActivityType(BaseModel):
    __tablename__ = "activity_type"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    type_name = Column(String(64), nullable=False, index=True)
    # ... 其他字段不变

# 修改 ActivityParticipant 类
class ActivityParticipant(BaseModel):
    __tablename__ = "activity_participants"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    activity_id = Column(Integer)
    # ... 其他字段不变

# 修改 CheckInRecord 类
class CheckInRecord(BaseModel):
    __tablename__ = "checkin_records"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    activity_id = Column(Integer)
    # ... 其他字段不变

# 修改 AdminActivityTypeRole 类
class AdminActivityTypeRole(BaseModel):
    __tablename__ = "admin_activity_type_role"
    tenant_id = Column(Integer, nullable=False, index=True, default=1)  # 新增
    admin_user_id = Column(Integer, nullable=False, index=True)
    # ... 其他字段不变
```

### 3.2 修改安全模块

**文件：`backend/app/core/security.py`**

```python
from datetime import datetime, timedelta
from jose import jwt
from app.core.config import settings

BCRYPT_MAX_BYTES = 72

def create_access_token(sub: str, role: str, tenant_id: int) -> str:
    """生成 JWT，包含租户信息"""
    expire = datetime.utcnow() + timedelta(minutes=settings.JWT_EXPIRE_MINUTES)
    payload = {
        "sub": str(sub),
        "role": role,
        "tenant_id": tenant_id,
        "exp": expire
    }
    return jwt.encode(payload, settings.JWT_SECRET, algorithm="HS256")

def decode_access_token(token: str) -> dict | None:
    """解析 JWT，返回包含 tenant_id 的 payload"""
    try:
        payload = jwt.decode(token, settings.JWT_SECRET, algorithms=["HS256"])
        return payload
    except jwt.JWTError:
        return None
```

### 3.3 修改鉴权依赖

**文件：`backend/app/api/deps.py`**

```python
from typing import Generator
from fastapi import Depends, HTTPException
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from app.database import SessionLocal
from app.core.security import decode_access_token
from sqlalchemy.orm import Session
from app.crud import crud_admin, crud_activity

security = HTTPBearer(auto_error=False)


def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def _parse_token(credentials: HTTPAuthorizationCredentials | None) -> dict | None:
    if not credentials or credentials.scheme != "Bearer":
        return None
    return decode_access_token(credentials.credentials)


class TenantContext:
    """租户上下文，包含当前用户的所有身份信息"""
    def __init__(self, user_id: int, role: str, tenant_id: int):
        self.user_id = user_id
        self.role = role
        self.tenant_id = tenant_id


def get_tenant_context(
    credentials: HTTPAuthorizationCredentials | None = Depends(security),
) -> TenantContext:
    """解析 JWT，返回租户上下文（所有需要鉴权的接口都应使用此依赖）"""
    payload = _parse_token(credentials)
    if not payload:
        raise HTTPException(status_code=401, detail="未登录或登录已过期")
    
    role = payload.get("role")
    if role not in ("admin", "user"):
        raise HTTPException(status_code=401, detail="无效的登录状态")
    
    try:
        user_id = int(payload["sub"])
        tenant_id = int(payload["tenant_id"])
    except (KeyError, ValueError):
        raise HTTPException(status_code=401, detail="无效的登录状态")
    
    return TenantContext(user_id=user_id, role=role, tenant_id=tenant_id)


def get_current_admin(
    ctx: TenantContext = Depends(get_tenant_context),
) -> TenantContext:
    """仅管理员可访问，返回租户上下文"""
    if ctx.role != "admin":
        raise HTTPException(status_code=401, detail="未登录或登录已过期")
    return ctx


def get_current_admin_optional(
    credentials: HTTPAuthorizationCredentials | None = Depends(security),
) -> TenantContext | None:
    """可选管理员鉴权：有 token 且为 admin 则返回上下文，否则 None"""
    payload = _parse_token(credentials)
    if not payload or payload.get("role") != "admin":
        return None
    try:
        user_id = int(payload["sub"])
        tenant_id = int(payload["tenant_id"])
        return TenantContext(user_id=user_id, role="admin", tenant_id=tenant_id)
    except (KeyError, ValueError):
        return None


def get_current_user(
    ctx: TenantContext = Depends(get_tenant_context),
) -> TenantContext:
    """任意已登录用户可访问，返回租户上下文"""
    return ctx


def get_current_user_optional(
    credentials: HTTPAuthorizationCredentials | None = Depends(security),
) -> TenantContext | None:
    """可选鉴权：有 token 则返回上下文，无 token 返回 None"""
    payload = _parse_token(credentials)
    if not payload:
        return None
    role = payload.get("role")
    if role not in ("admin", "user"):
        return None
    try:
        user_id = int(payload["sub"])
        tenant_id = int(payload["tenant_id"])
        return TenantContext(user_id=user_id, role=role, tenant_id=tenant_id)
    except (KeyError, ValueError):
        return None


def get_admin_scope(
    db: Session = Depends(get_db),
    ctx: TenantContext = Depends(get_current_admin),
) -> dict:
    """
    返回管理员权限范围（包含租户信息）
    返回值: {"admin_id": int, "is_super": bool, "allowed_activity_type_ids": list[int], "tenant_id": int}
    """
    is_super, allowed_ids = crud_admin.get_admin_scope(db, ctx.user_id, ctx.tenant_id)
    return {
        "admin_id": ctx.user_id,
        "is_super": is_super,
        "allowed_activity_type_ids": allowed_ids,
        "tenant_id": ctx.tenant_id
    }


def require_activity_admin(
    activity_id: int,
    db: Session = Depends(get_db),
    ctx: TenantContext = Depends(get_current_admin),
) -> int:
    """
    校验当前管理员是否有该活动的管理权限
    返回 activity_id；无权限则 403
    """
    activity = crud_activity.get_activity(db, activity_id, ctx.tenant_id)
    if not activity:
        raise HTTPException(status_code=404, detail="活动不存在")
    
    # 租户隔离：活动必须属于当前租户
    if activity.tenant_id != ctx.tenant_id:
        raise HTTPException(status_code=403, detail="无该活动的访问权限")
    
    is_super, allowed_ids = crud_admin.get_admin_scope(db, ctx.user_id, ctx.tenant_id)
    if is_super:
        return activity_id
    
    atid = getattr(activity, "activity_type_id", None)
    if atid is not None and atid in allowed_ids:
        return activity_id
    
    raise HTTPException(status_code=403, detail="无该活动的管理权限")
```

### 3.4 新增租户 CRUD

**文件：`backend/app/crud/crud_tenant.py`**（新建）

```python
from sqlalchemy.orm import Session
from app.schemas import Tenant
from fastapi import HTTPException
from datetime import datetime


def get_tenant(db: Session, tenant_id: int) -> Tenant | None:
    """根据 ID 获取租户"""
    return db.query(Tenant).filter(Tenant.id == tenant_id).first()


def get_tenant_by_code(db: Session, code: str) -> Tenant | None:
    """根据 code 获取租户"""
    return db.query(Tenant).filter(Tenant.code == code).first()


def create_tenant(
    db: Session,
    name: str,
    code: str,
    plan: str = "basic",
    contact_name: str | None = None,
    contact_phone: str | None = None,
) -> Tenant:
    """创建租户"""
    existing = get_tenant_by_code(db, code)
    if existing:
        raise HTTPException(status_code=400, detail="租户编码已存在")
    
    db_tenant = Tenant(
        name=name,
        code=code,
        status=1,
        plan=plan,
        contact_name=contact_name,
        contact_phone=contact_phone,
    )
    db.add(db_tenant)
    db.commit()
    db.refresh(db_tenant)
    return db_tenant


def check_tenant_active(db: Session, tenant_id: int) -> bool:
    """检查租户是否有效（状态正常且未过期）"""
    tenant = get_tenant(db, tenant_id)
    if not tenant:
        return False
    if tenant.status != 1:
        return False
    if tenant.expire_at and tenant.expire_at < datetime.now():
        return False
    return True


def update_tenant_status(db: Session, tenant_id: int, status: int) -> Tenant:
    """更新租户状态"""
    tenant = get_tenant(db, tenant_id)
    if not tenant:
        raise HTTPException(status_code=404, detail="租户不存在")
    tenant.status = status
    db.commit()
    db.refresh(tenant)
    return tenant
```

### 3.5 修改现有 CRUD（添加租户过滤）

**文件：`backend/app/crud/crud_activity.py`**

```python
from sqlalchemy.orm import Session
from sqlalchemy import desc
from datetime import datetime
from typing import List, Optional
from fastapi import HTTPException
from app.models.activity import ActivityCreate, ActivityUpdate
from app.schemas import Activity, ActivityParticipant, CheckInRecord
from app.crud import crud_activity_type


def create_activity(db: Session, activity: ActivityCreate, tenant_id: int) -> Activity:
    """创建活动（需指定租户）"""
    try:
        participants = activity.participants or []
        activity_type_id = None
        
        # 解析活动类型
        if activity.activity_type_id is not None:
            t = crud_activity_type.get_by_id(db, activity.activity_type_id, tenant_id)
            if t and t.tenant_id == tenant_id:
                activity_type_id = t.id
        
        activity_dict = activity.model_dump(exclude={"participants", "activity_type_name"})
        activity_dict["activity_type_id"] = activity_type_id
        activity_dict["tenant_id"] = tenant_id  # 设置租户ID
        activity_dict["status"] = 1
        activity_dict["create_time"] = datetime.now()
        activity_dict["update_time"] = datetime.now()

        db_activity = Activity(**activity_dict)
        db.add(db_activity)
        db.flush()

        for participant in participants:
            participant_dict = {
                "tenant_id": tenant_id,  # 设置租户ID
                "activity_id": db_activity.id,
                "participant_name": participant.participant_name,
                "identity_number": participant.identity_number or "",
                "phone": participant.phone,
                "create_time": datetime.now(),
                "update_time": datetime.now(),
                "user_id": getattr(participant, "user_id", None),
            }
            db_participant = ActivityParticipant(**participant_dict)
            db.add(db_participant)

        db.commit()
        db.refresh(db_activity)
        return db_activity
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))


def get_activity(db: Session, activity_id: int, tenant_id: int) -> Optional[Activity]:
    """获取单个活动（租户隔离）"""
    return db.query(Activity).filter(
        Activity.id == activity_id,
        Activity.tenant_id == tenant_id
    ).first()


def get_activities(
    db: Session,
    tenant_id: int,  # 新增参数
    skip: int = 0,
    limit: int = 10,
    status: Optional[int] = None,
    allowed_activity_type_ids: Optional[List[int]] = None,
) -> tuple:
    """获取活动列表（租户隔离）"""
    try:
        query = db.query(Activity).filter(Activity.tenant_id == tenant_id)  # 租户过滤
        
        if status is not None:
            query = query.filter(Activity.status == status)
        if allowed_activity_type_ids is not None and len(allowed_activity_type_ids) > 0:
            query = query.filter(Activity.activity_type_id.in_(allowed_activity_type_ids))
        
        total = query.count()
        activities = query.order_by(desc(Activity.start_time)).offset(skip).limit(limit).all()
        return activities, total
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))


def update_activity_status(
    db: Session,
    activity_id: int,
    status: int,
    tenant_id: int,  # 新增参数
) -> Activity:
    """更新活动状态（租户隔离）"""
    try:
        db_activity = get_activity(db, activity_id, tenant_id)
        if not db_activity:
            raise HTTPException(status_code=404, detail="Activity not found")
        
        current_time = datetime.now()
        
        if status == 2:
            if current_time < db_activity.start_time:
                raise HTTPException(status_code=400, detail="Cannot start activity before start time")
        elif status == 3:
            db_activity.end_time = current_time
        
        db_activity.status = status
        db.commit()
        db.refresh(db_activity)
        return db_activity
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))


def update_activity(
    db: Session,
    activity_id: int,
    activity_update: ActivityUpdate,
    tenant_id: int,  # 新增参数
) -> Activity:
    """更新活动信息（租户隔离）"""
    try:
        db_activity = get_activity(db, activity_id, tenant_id)
        if not db_activity:
            raise HTTPException(status_code=404, detail="Activity not found")
        
        update_data = activity_update.model_dump(exclude_unset=True)
        
        if "activity_type_name" in update_data:
            type_name = update_data.pop("activity_type_name")
            if type_name:
                t = crud_activity_type.get_or_create_by_name(db, type_name.strip(), tenant_id)
                update_data["activity_type_id"] = t.id
        
        for field, value in update_data.items():
            if value is not None:
                setattr(db_activity, field, value)
        
        db_activity.update_time = datetime.now()
        db.commit()
        db.refresh(db_activity)
        return db_activity
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))


def delete_activity(db: Session, activity_id: int, tenant_id: int) -> bool:
    """删除活动（租户隔离）"""
    try:
        db_activity = get_activity(db, activity_id, tenant_id)
        if not db_activity:
            raise HTTPException(status_code=404, detail="Activity not found")
        
        # 删除相关参与人（租户隔离）
        db.query(ActivityParticipant).filter(
            ActivityParticipant.activity_id == activity_id,
            ActivityParticipant.tenant_id == tenant_id
        ).delete()
        
        # 删除相关签到记录（租户隔离）
        db.query(CheckInRecord).filter(
            CheckInRecord.activity_id == activity_id,
            CheckInRecord.tenant_id == tenant_id
        ).delete()
        
        db.delete(db_activity)
        db.commit()
        return True
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))
```

**文件：`backend/app/crud/crud_user.py`**

```python
from app.models.user import UserCreate, RegisterRequest
from sqlalchemy.orm import Session
from app.schemas import User
from app.core.security import hash_password, verify_password
from fastapi import HTTPException


def get_users(db: Session, tenant_id: int) -> list[User]:
    """获取用户列表（租户隔离）"""
    return db.query(User).filter(User.tenant_id == tenant_id).all()


def get_user(db: Session, user_id: int, tenant_id: int) -> User | None:
    """获取单个用户（租户隔离）"""
    return db.query(User).filter(
        User.id == user_id,
        User.tenant_id == tenant_id
    ).first()


def get_user_by_phone(db: Session, phone: str, tenant_id: int) -> User | None:
    """根据手机号获取用户（租户隔离）"""
    return db.query(User).filter(
        User.phone == phone,
        User.tenant_id == tenant_id
    ).first()


def get_user_by_wx_openid(db: Session, openid: str, tenant_id: int) -> User | None:
    """根据微信 openid 获取用户（租户隔离）"""
    return db.query(User).filter(
        User.wx_openid == openid,
        User.tenant_id == tenant_id
    ).first()


def get_or_create_user_wechat(db: Session, openid: str, tenant_id: int, nickname: str | None = None) -> User:
    """微信授权登录：存在则返回，不存在则创建"""
    user = get_user_by_wx_openid(db, openid, tenant_id)
    if user:
        return user
    try:
        phone_placeholder = f"wx_{openid}"
        db_user = User(
            tenant_id=tenant_id,  # 设置租户ID
            name=nickname or "微信用户",
            phone=phone_placeholder,
            email=None,
            password_hash=None,
            identity_number=None,
            sex=None,
            isblock=0,
            block_reason=None,
            wx_openid=openid,
        )
        db.add(db_user)
        db.commit()
        db.refresh(db_user)
        return db_user
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=f"创建微信用户失败: {str(e)}")


def create_user(db: Session, user: UserCreate, tenant_id: int) -> User:
    """创建用户（租户隔离）"""
    try:
        if user.identity_number:
            existing = db.query(User).filter(
                User.identity_number == user.identity_number,
                User.tenant_id == tenant_id
            ).first()
            if existing:
                raise HTTPException(status_code=400, detail="该证件号已存在")
        
        db_user = User(
            tenant_id=tenant_id,  # 设置租户ID
            **user.model_dump()
        )
        db.add(db_user)
        db.commit()
        db.refresh(db_user)
        return db_user
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))


def register_user(db: Session, body: RegisterRequest, tenant_id: int) -> User:
    """用户注册（租户隔离）"""
    try:
        existing_phone = get_user_by_phone(db, body.phone.strip(), tenant_id)
        if existing_phone:
            raise HTTPException(status_code=400, detail="该手机号已注册")
        
        if body.email:
            existing_email = db.query(User).filter(
                User.email == body.email,
                User.tenant_id == tenant_id
            ).first()
            if existing_email:
                raise HTTPException(status_code=400, detail="该邮箱已注册")
        
        db_user = User(
            tenant_id=tenant_id,  # 设置租户ID
            name=body.name.strip(),
            phone=body.phone.strip(),
            email=body.email,
            password_hash=hash_password(body.password),
            identity_number=None,
            sex=None,
            isblock=0,
            block_reason=None,
        )
        db.add(db_user)
        db.commit()
        db.refresh(db_user)
        return db_user
    except HTTPException:
        raise
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=400, detail=str(e))


def authenticate_user(db: Session, phone: str, password: str, tenant_id: int) -> User | None:
    """普通用户认证（租户隔离）"""
    user = get_user_by_phone(db, phone, tenant_id)
    if not user or not user.password_hash:
        return None
    if not verify_password(password, user.password_hash):
        return None
    return user
```

**文件：`backend/app/crud/crud_admin.py`**

```python
from sqlalchemy.orm import Session
from app.schemas import AdminUser, AdminActivityTypeRole
from app.core.security import verify_password
from typing import List


def get_admin_by_username(db: Session, username: str, tenant_id: int) -> AdminUser | None:
    """根据用户名获取管理员（租户隔离）"""
    return db.query(AdminUser).filter(
        AdminUser.username == username,
        AdminUser.tenant_id == tenant_id
    ).first()


def get_admin_by_id(db: Session, admin_id: int, tenant_id: int) -> AdminUser | None:
    """根据ID获取管理员（租户隔离）"""
    return db.query(AdminUser).filter(
        AdminUser.id == admin_id,
        AdminUser.tenant_id == tenant_id
    ).first()


def authenticate_admin(db: Session, username: str, password: str, tenant_id: int) -> AdminUser | None:
    """管理员认证（租户隔离）"""
    admin = get_admin_by_username(db, username, tenant_id)
    if not admin:
        return None
    if not verify_password(password, admin.password_hash):
        return None
    return admin


def get_admin_scope(db: Session, admin_id: int, tenant_id: int) -> tuple[bool, List[int]]:
    """
    获取管理员权限范围（租户隔离）
    返回: (is_super: bool, allowed_activity_type_ids: List[int])
    """
    admin = get_admin_by_id(db, admin_id, tenant_id)
    if not admin:
        return False, []
    
    is_super = getattr(admin, "is_super_admin", 0) == 1
    if is_super:
        return True, []
    
    roles = db.query(AdminActivityTypeRole).filter(
        AdminActivityTypeRole.admin_user_id == admin_id,
        AdminActivityTypeRole.tenant_id == tenant_id
    ).all()
    
    allowed_ids = [r.activity_type_id for r in roles]
    return False, allowed_ids
```

**文件：`backend/app/crud/crud_activity_type.py`**

```python
from sqlalchemy.orm import Session
from app.schemas import ActivityType
from fastapi import HTTPException


def get_by_id(db: Session, type_id: int, tenant_id: int) -> ActivityType | None:
    """根据ID获取活动类型（租户隔离）"""
    return db.query(ActivityType).filter(
        ActivityType.id == type_id,
        ActivityType.tenant_id == tenant_id
    ).first()


def get_by_name(db: Session, name: str, tenant_id: int) -> ActivityType | None:
    """根据名称获取活动类型（租户隔离）"""
    return db.query(ActivityType).filter(
        ActivityType.type_name == name.strip(),
        ActivityType.tenant_id == tenant_id
    ).first()


def get_or_create_by_name(db: Session, name: str, tenant_id: int) -> ActivityType:
    """获取或创建活动类型（租户隔离）"""
    t = get_by_name(db, name, tenant_id)
    if t:
        return t
    
    db_type = ActivityType(
        tenant_id=tenant_id,
        type_name=name.strip(),
        code=None,
    )
    db.add(db_type)
    db.commit()
    db.refresh(db_type)
    return db_type


def get_all(db: Session, tenant_id: int) -> list[ActivityType]:
    """获取所有活动类型（租户隔离）"""
    return db.query(ActivityType).filter(
        ActivityType.tenant_id == tenant_id
    ).all()
```

**文件：`backend/app/crud/crud_checkin.py`**

```python
from sqlalchemy.orm import Session
from app.schemas import CheckInRecord
from typing import List, Optional
from datetime import datetime


def get_activity_checkins(
    db: Session, 
    activity_id: int, 
    tenant_id: int,  # 新增参数
    skip: int = 0, 
    limit: int = 100
) -> List[CheckInRecord]:
    """获取活动的签到记录（租户隔离）"""
    return db.query(CheckInRecord).filter(
        CheckInRecord.activity_id == activity_id,
        CheckInRecord.tenant_id == tenant_id
    ).offset(skip).limit(limit).all()


def get_checkins(
    db: Session,
    tenant_id: int,  # 新增参数
    skip: int = 0,
    limit: int = 100,
    activity_id: Optional[int] = None,
) -> List[CheckInRecord]:
    """获取签到记录列表（租户隔离）"""
    query = db.query(CheckInRecord).filter(CheckInRecord.tenant_id == tenant_id)
    if activity_id is not None:
        query = query.filter(CheckInRecord.activity_id == activity_id)
    return query.offset(skip).limit(limit).all()


def create_checkin(
    db: Session,
    activity_id: int,
    name: str,
    phone: str,
    identity_number: str,
    tenant_id: int,  # 新增参数
    user_id: Optional[int] = None,
    has_attend: int = 1,
    note: Optional[str] = None,
) -> CheckInRecord:
    """创建签到记录（租户隔离）"""
    db_checkin = CheckInRecord(
        tenant_id=tenant_id,
        activity_id=activity_id,
        user_id=user_id,
        name=name,
        phone=phone,
        identity_number=identity_number,
        checkin_time=datetime.now(),
        has_attend=has_attend,
        note=note,
        create_time=datetime.now(),
        update_time=datetime.now(),
    )
    db.add(db_checkin)
    db.commit()
    db.refresh(db_checkin)
    return db_checkin
```

**文件：`backend/app/crud/crud_participant.py`**

```python
from sqlalchemy.orm import Session
from app.schemas import ActivityParticipant
from typing import List


def get_participants_by_activity(
    db: Session, 
    activity_id: int, 
    tenant_id: int,  # 新增参数
    skip: int = 0, 
    limit: int = 100
) -> List[ActivityParticipant]:
    """获取活动的参与人列表（租户隔离）"""
    return db.query(ActivityParticipant).filter(
        ActivityParticipant.activity_id == activity_id,
        ActivityParticipant.tenant_id == tenant_id
    ).offset(skip).limit(limit).all()


def create_participant(
    db: Session,
    activity_id: int,
    participant_name: str,
    tenant_id: int,  # 新增参数
    phone: str = None,
    identity_number: str = None,
    user_id: int = None,
) -> ActivityParticipant:
    """创建参与人（租户隔离）"""
    participant = ActivityParticipant(
        tenant_id=tenant_id,
        activity_id=activity_id,
        participant_name=participant_name,
        phone=phone,
        identity_number=identity_number or "",
        user_id=user_id,
    )
    db.add(participant)
    db.commit()
    db.refresh(participant)
    return participant


def get_activity_statistics(db: Session, activity_id: int, tenant_id: int) -> dict:
    """获取活动统计（租户隔离）"""
    participants = get_participants_by_activity(db, activity_id, tenant_id, limit=10000)
    return {
        "total": len(participants),
        "activity_id": activity_id,
    }
```

### 3.6 修改 API 路由

**重要：未登录接口的租户识别原则**

对于签到、报名等允许未登录用户访问的接口，**必须通过 activity_id 查询活动所属租户**，不能使用默认租户。

```python
# 错误做法：未登录时默认 tenant_id=1
tenant_id = ctx.tenant_id if ctx else 1  # ❌ 会导致数据归属错误

# 正确做法：通过活动获取租户
activity = crud_activity.get_activity_by_id(db, activity_id)
if not activity:
    raise HTTPException(status_code=404, detail="活动不存在")
tenant_id = activity.tenant_id  # ✅ 从活动获取正确租户
```

**文件：`backend/app/api/v1/endpoints/auth.py`**

```python
import logging
import time
from collections import defaultdict
from urllib.request import urlopen, Request as UrllibRequest
from urllib.error import HTTPError, URLError
import json

from fastapi import APIRouter, Depends, HTTPException, Request
from sqlalchemy.orm import Session
from pydantic import BaseModel

from app.api import deps
from app.core.config import settings
from app.crud import crud_admin, crud_user, crud_tenant
from app.core.security import create_access_token, BCRYPT_MAX_BYTES
from app.models.user import UserLoginRequest, UserLoginResponse
from app.schemas import ActivityType

logger = logging.getLogger(__name__)
router = APIRouter()

_login_attempts: dict[str, list[float]] = defaultdict(list)


def _get_client_ip(request: Request) -> str:
    forwarded = request.headers.get("X-Forwarded-For")
    if forwarded:
        return forwarded.split(",")[0].strip()
    return request.client.host if request.client else "unknown"


def _is_https(request: Request) -> bool:
    proto = request.headers.get("X-Forwarded-Proto", "").strip().lower()
    if proto == "https":
        return True
    return request.url.scheme == "https"


def _check_login_rate_limit(ip: str) -> None:
    now = time.time()
    window = settings.LOGIN_RATE_LIMIT_WINDOW_SECONDS
    max_count = settings.LOGIN_RATE_LIMIT_COUNT
    _login_attempts[ip] = [t for t in _login_attempts[ip] if now - t < window]
    if len(_login_attempts[ip]) >= max_count:
        raise HTTPException(
            status_code=429,
            detail=f"登录尝试过于频繁，请 {window} 秒后再试",
        )
    _login_attempts[ip].append(now)


def _enforce_https_and_rate_limit(request: Request) -> None:
    if settings.REQUIRE_HTTPS_FOR_LOGIN and not _is_https(request):
        raise HTTPException(status_code=403, detail="登录仅支持 HTTPS，请使用安全连接")
    _check_login_rate_limit(_get_client_ip(request))


def _check_password_length(password: str) -> None:
    pwd_bytes = len(password.encode("utf-8"))
    if pwd_bytes > BCRYPT_MAX_BYTES:
        raise HTTPException(
            status_code=400,
            detail=f"密码长度不能超过 {BCRYPT_MAX_BYTES} 字节",
        )


class LoginRequest(BaseModel):
    username: str
    password: str
    tenant_code: str | None = None  # 新增：租户编码（可选，默认为 default）


class ActivityTypeItem(BaseModel):
    id: int
    name: str
    code: str | None = None

    class Config:
        from_attributes = True


class LoginResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    role: str = "admin"
    tenant_id: int  # 新增
    tenant_name: str  # 新增
    is_super_admin: bool = True
    activity_types: list[ActivityTypeItem] = []


def _admin_activity_types_for_response(db: Session, admin_id: int, tenant_id: int) -> list[dict]:
    """返回该管理员可管理的活动类型列表（租户隔离）"""
    is_super, allowed_ids = crud_admin.get_admin_scope(db, admin_id, tenant_id)
    if is_super or not allowed_ids:
        return []
    types = db.query(ActivityType).filter(
        ActivityType.id.in_(allowed_ids),
        ActivityType.tenant_id == tenant_id
    ).all()
    return [{"id": t.id, "name": t.type_name, "code": t.code} for t in types]


@router.post("/login", response_model=LoginResponse)
def login(
    request: Request,
    body: LoginRequest,
    db: Session = Depends(deps.get_db),
):
    """管理员登录：用户名 + 密码 + 租户编码"""
    _enforce_https_and_rate_limit(request)
    _check_password_length(body.password)
    
    # 获取租户
    tenant_code = body.tenant_code or "default"
    tenant = crud_tenant.get_tenant_by_code(db, tenant_code)
    if not tenant:
        raise HTTPException(status_code=400, detail="租户不存在")
    if tenant.status != 1:
        raise HTTPException(status_code=403, detail="租户已禁用或已过期")
    
    # 认证管理员
    try:
        admin = crud_admin.authenticate_admin(db, body.username, body.password, tenant.id)
    except ValueError as e:
        if "72" in str(e) or "bytes" in str(e).lower():
            raise HTTPException(status_code=400, detail="密码校验异常，请确认密码长度正常") from e
        raise
    
    if not admin:
        raise HTTPException(status_code=401, detail="用户名或密码错误")
    
    # 生成带租户信息的 token
    token = create_access_token(sub=str(admin.id), role="admin", tenant_id=tenant.id)
    is_super = getattr(admin, "is_super_admin", 0) == 1
    activity_types = _admin_activity_types_for_response(db, admin.id, tenant.id)
    
    return LoginResponse(
        access_token=token,
        tenant_id=tenant.id,
        tenant_name=tenant.name,
        is_super_admin=is_super,
        activity_types=[ActivityTypeItem(**t) for t in activity_types],
    )


@router.post("/user-login", response_model=UserLoginResponse)
def user_login(
    request: Request,
    body: UserLoginRequest,
    db: Session = Depends(deps.get_db),
):
    """普通用户登录：手机号 + 密码（需要指定租户）"""
    _enforce_https_and_rate_limit(request)
    _check_password_length(body.password)
    
    # 普通用户登录需要从请求头或请求体获取租户信息
    # 这里假设前端会传递 tenant_code（实际项目中可根据子域名等方式获取）
    tenant_code = getattr(body, 'tenant_code', None) or 'default'
    tenant = crud_tenant.get_tenant_by_code(db, tenant_code)
    if not tenant or tenant.status != 1:
        raise HTTPException(status_code=400, detail="租户不存在或已禁用")
    
    user = crud_user.authenticate_user(db, body.phone.strip(), body.password, tenant.id)
    if not user:
        raise HTTPException(status_code=401, detail="手机号或密码错误")
    if user.isblock == 1:
        reason = user.block_reason or "账号已被禁用"
        raise HTTPException(status_code=403, detail=f"账号已被拉黑：{reason}")
    
    token = create_access_token(sub=str(user.id), role="user", tenant_id=tenant.id)
    return UserLoginResponse(
        access_token=token,
        user_id=user.id,
        user_name=user.name or "",
    )


class WeChatLoginRequest(BaseModel):
    code: str
    tenant_code: str | None = None  # 新增


def _wechat_code2session(code: str) -> dict:
    appid = settings.WECHAT_APPID
    secret = settings.WECHAT_SECRET
    if not appid or not secret:
        raise HTTPException(
            status_code=503,
            detail="服务未配置微信登录，请设置 WECHAT_APPID 与 WECHAT_SECRET",
        )
    url = (
        f"https://api.weixin.qq.com/sns/jscode2session"
        f"?appid={appid}&secret={secret}&js_code={code}&grant_type=authorization_code"
    )
    try:
        req = UrllibRequest(url, method="GET")
        with urlopen(req, timeout=10) as resp:
            data = json.loads(resp.read().decode())
    except (HTTPError, URLError, json.JSONDecodeError) as e:
        logger.exception("wechat jscode2session error: %s", e)
        raise HTTPException(status_code=502, detail="微信服务暂时不可用，请稍后重试")
    errcode = data.get("errcode", 0)
    if errcode != 0:
        errmsg = data.get("errmsg", "unknown")
        detail = f"微信登录失败：{errmsg}"
        if errcode == 40029 or "invalid code" in (errmsg or "").lower():
            detail += "。请检查小程序配置。"
        raise HTTPException(status_code=400, detail=detail)
    openid = data.get("openid")
    if not openid:
        raise HTTPException(status_code=400, detail="微信未返回 openid")
    return data


@router.post("/wechat-login", response_model=UserLoginResponse)
def wechat_login(
    request: Request,
    body: WeChatLoginRequest,
    db: Session = Depends(deps.get_db),
):
    """微信小程序授权登录（租户隔离）"""
    _check_login_rate_limit(_get_client_ip(request))
    code = (body.code or "").strip()
    if not code:
        raise HTTPException(status_code=400, detail="缺少 code")
    
    data = _wechat_code2session(code)
    openid = data["openid"]
    
    # 获取租户
    tenant_code = body.tenant_code or "default"
    tenant = crud_tenant.get_tenant_by_code(db, tenant_code)
    if not tenant or tenant.status != 1:
        raise HTTPException(status_code=400, detail="租户不存在或已禁用")
    
    user = crud_user.get_or_create_user_wechat(db, openid, tenant.id, nickname=None)
    if user.isblock == 1:
        reason = user.block_reason or "账号已被禁用"
        raise HTTPException(status_code=403, detail=f"账号已被拉黑：{reason}")
    
    token = create_access_token(sub=str(user.id), role="user", tenant_id=tenant.id)
    return UserLoginResponse(
        access_token=token,
        user_id=user.id,
        user_name=user.name or "微信用户",
    )
```

**文件：`backend/app/api/v1/endpoints/activities.py`**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List

from app.crud import crud_activity, crud_checkin, crud_participant, crud_admin
from app.models import activity, checkin
from app.api import deps

router = APIRouter()


def _allowed_type_ids_for_list(db: Session, admin_id: int, tenant_id: int) -> List[int] | None:
    """管理员列表过滤（租户隔离）"""
    if admin_id is None:
        return None
    is_super, allowed = crud_admin.get_admin_scope(db, admin_id, tenant_id)
    if is_super or not allowed:
        return None
    return allowed


@router.post("/", response_model=activity.ActivityResponse)
def create_activity(
    body: activity.ActivityCreate,
    db: Session = Depends(deps.get_db),
    scope: dict = Depends(deps.get_admin_scope),
):
    """创建活动（租户隔离）"""
    tenant_id = scope["tenant_id"]
    
    if not scope["is_super"]:
        allowed = scope["allowed_activity_type_ids"]
        if not allowed:
            raise HTTPException(status_code=403, detail="当前账号未授权任何活动类型，无法创建活动")
        from app.crud import crud_activity_type
        type_id = body.activity_type_id
        type_name = (body.activity_type_name or "").strip()
        if type_id is not None:
            if type_id not in allowed:
                raise HTTPException(status_code=403, detail="所选活动类型不在授权范围内")
        elif type_name:
            t = crud_activity_type.get_by_name(db, type_name, tenant_id)
            if not t or t.id not in allowed:
                raise HTTPException(status_code=403, detail="所选活动类型不在授权范围内或不存在")
        else:
            raise HTTPException(status_code=400, detail="活动管理员创建活动须指定活动类型")
    
    return crud_activity.create_activity(db=db, activity=body, tenant_id=tenant_id)


@router.get("/", response_model=activity.ActivityListResponse)
def list_activities(
    skip: int = 0,
    limit: int = 100,
    status: int = None,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext | None = Depends(deps.get_current_admin_optional),
):
    """活动列表（租户隔离）"""
    tenant_id = ctx.tenant_id if ctx else 1  # 默认租户
    admin_id = ctx.user_id if ctx else None
    
    allowed = _allowed_type_ids_for_list(db, admin_id, tenant_id) if admin_id else None
    activities, total = crud_activity.get_activities(
        db, tenant_id=tenant_id, skip=skip, limit=limit, status=status,
        allowed_activity_type_ids=allowed,
    )
    return {"items": activities, "total": total}


@router.get("/unstarted/", response_model=activity.ActivityListResponse)
def get_unstarted_activities(
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext | None = Depends(deps.get_current_admin_optional),
):
    """未开始活动列表（租户隔离）"""
    tenant_id = ctx.tenant_id if ctx else 1
    admin_id = ctx.user_id if ctx else None
    
    allowed = _allowed_type_ids_for_list(db, admin_id, tenant_id) if admin_id else None
    activities, total = crud_activity.get_activities(
        db, tenant_id=tenant_id, status=1, allowed_activity_type_ids=allowed
    )
    return {"items": activities, "total": total}


@router.put("/{activity_id}/status")
def update_activity_status(
    activity_id: int,
    status: int,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """更新活动状态（租户隔离）"""
    # require_activity_admin 已包含租户校验
    _ = deps.require_activity_admin(activity_id, db, ctx)
    act = crud_activity.update_activity_status(db, activity_id, status, ctx.tenant_id)
    if act is None:
        raise HTTPException(status_code=404, detail="Activity not found")
    return {"status": "success", "message": "Activity status updated"}


@router.get("/{activity_id}/checkins/", response_model=List[checkin.CheckInResponse])
def get_activity_checkins(
    activity_id: int,
    skip: int = 0,
    limit: int = 100,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """查看活动签到（租户隔离）"""
    _ = deps.require_activity_admin(activity_id, db, ctx)
    return crud_checkin.get_activity_checkins(db, activity_id, ctx.tenant_id, skip=skip, limit=limit)


@router.get("/{activity_id}/statistics/")
def get_activity_stats(
    activity_id: int,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """活动报名统计（租户隔离）"""
    _ = deps.require_activity_admin(activity_id, db, ctx)
    return crud_participant.get_activity_statistics(db, activity_id, ctx.tenant_id)


@router.put("/{activity_id}", response_model=activity.ActivityResponse)
def update_activity(
    activity_id: int,
    body: activity.ActivityUpdate,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """编辑活动信息（租户隔离）"""
    _ = deps.require_activity_admin(activity_id, db, ctx)
    return crud_activity.update_activity(db, activity_id, body, ctx.tenant_id)


@router.delete("/{activity_id}")
def delete_activity(
    activity_id: int,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """删除活动（租户隔离）"""
    _ = deps.require_activity_admin(activity_id, db, ctx)
    crud_activity.delete_activity(db, activity_id, ctx.tenant_id)
    return {"status": "success", "message": "Activity deleted successfully"}
```

**文件：`backend/app/api/v1/endpoints/users.py`**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.crud import crud_user
from app.api import deps
from app.models.user import UserCreate, RegisterRequest, UserResponse
from typing import List

router = APIRouter()


@router.post("/register", response_model=UserResponse)
def register(
    body: RegisterRequest,
    db: Session = Depends(deps.get_db),
):
    """用户注册（需要指定租户，这里默认使用 default）"""
    # 实际项目中，租户信息应该从请求头、子域名或请求体获取
    tenant_code = getattr(body, 'tenant_code', None) or 'default'
    from app.crud import crud_tenant
    tenant = crud_tenant.get_tenant_by_code(db, tenant_code)
    if not tenant or tenant.status != 1:
        raise HTTPException(status_code=400, detail="租户不存在或已禁用")
    
    user = crud_user.register_user(db, body, tenant.id)
    return user


@router.get("/me", response_model=UserResponse)
def get_me(
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_user),
):
    """获取当前用户信息（租户隔离）"""
    user = crud_user.get_user(db, ctx.user_id, ctx.tenant_id)
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    return user


@router.get("/", response_model=List[UserResponse])
def list_users(
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """用户列表（租户隔离，仅管理员）"""
    return crud_user.get_users(db, ctx.tenant_id)


@router.get("/{user_id}", response_model=UserResponse)
def get_user(
    user_id: int,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """获取用户详情（租户隔离）"""
    user = crud_user.get_user(db, user_id, ctx.tenant_id)
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    return user


@router.post("/create", response_model=UserResponse)
def create_user(
    body: UserCreate,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """创建用户（租户隔离，仅管理员）"""
    return crud_user.create_user(db, body, ctx.tenant_id)
```

**文件：`backend/app/api/v1/endpoints/checkins.py`**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.crud import crud_checkin, crud_activity
from app.api import deps
from app.models.checkin import CheckInCreate

router = APIRouter()


@router.post("/")
def create_checkin(
    body: CheckInCreate,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext | None = Depends(deps.get_current_user_optional),
):
    """签到（租户隔离）
    
    租户识别逻辑：
    1. 已登录用户：从 JWT 获取 tenant_id
    2. 未登录用户：从 activity_id 查询活动所属租户
    """
    # 验证活动存在并获取租户信息
    activity = crud_activity.get_activity_by_id(db, body.activity_id)
    if not activity:
        raise HTTPException(status_code=404, detail="活动不存在")
    if activity.status == 3:
        raise HTTPException(status_code=400, detail="活动已结束，无法签到")
    
    # 确定租户：优先从 token 获取，其次从活动获取
    tenant_id = ctx.tenant_id if ctx else activity.tenant_id
    
    # 安全检查：已登录用户的租户必须与活动租户一致
    if ctx and ctx.tenant_id != activity.tenant_id:
        raise HTTPException(status_code=403, detail="无权参与该活动")
    
    checkin = crud_checkin.create_checkin(
        db,
        activity_id=body.activity_id,
        name=body.name,
        phone=body.phone,
        identity_number=body.identity_number,
        tenant_id=tenant_id,
        user_id=ctx.user_id if ctx else None,
    )
    return {"status": "success", "checkin_id": checkin.id}


@router.get("/")
def list_checkins(
    skip: int = 0,
    limit: int = 100,
    activity_id: int = None,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """签到记录列表（租户隔离，仅管理员）"""
    return crud_checkin.get_checkins(
        db, tenant_id=ctx.tenant_id, skip=skip, limit=limit, activity_id=activity_id
    )
```

**文件：`backend/app/api/v1/endpoints/participants.py`**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.crud import crud_participant, crud_user, crud_activity
from app.api import deps
from app.models.participant import ParticipantCreate

router = APIRouter()


@router.post("/")
def create_participant(
    body: ParticipantCreate,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext | None = Depends(deps.get_current_user_optional),
):
    """报名（租户隔离，含黑名单检查）
    
    租户识别逻辑：
    1. 已登录用户：从 JWT 获取 tenant_id
    2. 未登录用户：从 activity_id 查询活动所属租户
    """
    # 验证活动存在并获取租户信息
    activity = crud_activity.get_activity_by_id(db, body.activity_id)
    if not activity:
        raise HTTPException(status_code=404, detail="活动不存在")
    if activity.status != 1:
        raise HTTPException(status_code=400, detail="活动已开始或已结束，无法报名")
    
    # 确定租户：优先从 token 获取，其次从活动获取
    tenant_id = ctx.tenant_id if ctx else activity.tenant_id
    
    # 安全检查：已登录用户的租户必须与活动租户一致
    if ctx and ctx.tenant_id != activity.tenant_id:
        raise HTTPException(status_code=403, detail="无权参与该活动")
    
    # 黑名单检查（在活动所属租户范围内检查）
    if body.phone:
        user = crud_user.get_user_by_phone(db, body.phone, tenant_id)
        if user and user.isblock == 1:
            reason = user.block_reason or "账号已被禁用"
            raise HTTPException(status_code=403, detail=f"报名失败：{reason}")
    
    participant = crud_participant.create_participant(
        db,
        activity_id=body.activity_id,
        participant_name=body.participant_name,
        tenant_id=tenant_id,
        phone=body.phone,
        identity_number=body.identity_number,
        user_id=ctx.user_id if ctx else None,
    )
    return {"status": "success", "participant_id": participant.id}


@router.get("/{activity_id}/")
def list_participants(
    activity_id: int,
    skip: int = 0,
    limit: int = 100,
    db: Session = Depends(deps.get_db),
    ctx: deps.TenantContext = Depends(deps.get_current_admin),
):
    """参与人列表（租户隔离，仅管理员）"""
    return crud_participant.get_participants_by_activity(
        db, activity_id, ctx.tenant_id, skip=skip, limit=limit
    )
```

---

## 四、租户识别机制

### 4.1 租户识别策略

| 场景 | 识别方式 | 实现方案 |
|------|----------|----------|
| 管理员登录 | 登录时指定 `tenant_code` | 登录表单增加租户选择 |
| 普通用户登录 | 从请求上下文获取 | 子域名 / 请求头 / App 配置 |
| 微信小程序登录 | AppId 绑定租户 | 数据库配置 `miniprogram_tenant` 表 |
| 未登录报名/签到 | 从活动获取租户 | 通过 `activity_id` 反查 |

### 4.2 微信小程序租户绑定

**新增数据库表：**

```sql
-- 小程序租户绑定表
CREATE TABLE `miniprogram_tenant` (
  `id` int NOT NULL AUTO_INCREMENT,
  `tenant_id` int NOT NULL COMMENT '租户ID',
  `appid` varchar(64) NOT NULL COMMENT '小程序 AppId',
  `app_name` varchar(100) DEFAULT NULL COMMENT '小程序名称',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_appid` (`appid`),
  INDEX `idx_tenant_id` (`tenant_id`)
) COMMENT='小程序租户绑定表';
```

**小程序登录改造：**

```python
@router.post("/wechat-login", response_model=UserLoginResponse)
def wechat_login(
    request: Request,
    body: WeChatLoginRequest,
    db: Session = Depends(deps.get_db),
):
    """微信小程序授权登录（通过 AppId 自动识别租户）"""
    _check_login_rate_limit(_get_client_ip(request))
    code = (body.code or "").strip()
    if not code:
        raise HTTPException(status_code=400, detail="缺少 code")
    
    data = _wechat_code2session(code)
    openid = data["openid"]
    
    # 通过 AppId 查询绑定的租户
    tenant = crud_tenant.get_tenant_by_appid(db, settings.WECHAT_APPID)
    if not tenant or tenant.status != 1:
        raise HTTPException(status_code=400, detail="租户不存在或已禁用")
    
    user = crud_user.get_or_create_user_wechat(db, openid, tenant.id, nickname=None)
    if user.isblock == 1:
        reason = user.block_reason or "账号已被禁用"
        raise HTTPException(status_code=403, detail=f"账号已被拉黑：{reason}")
    
    token = create_access_token(sub=str(user.id), role="user", tenant_id=tenant.id)
    return UserLoginResponse(
        access_token=token,
        user_id=user.id,
        user_name=user.name or "微信用户",
    )
```

---

## 五、前端改造

### 5.1 登录页面改造

**文件：`frontend/src/pages/LoginPage.tsx`**

需要添加租户选择（如果支持多租户登录）：

```tsx
import { useState } from 'react';

interface LoginFormData {
  username: string;
  password: string;
  tenant_code: string;
}

// 登录接口调用
const handleLogin = async (data: LoginFormData) => {
  const response = await fetch('/api/v1/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });
  
  const result = await response.json();
  if (result.access_token) {
    // 存储 token 和租户信息
    localStorage.setItem('admin_token', result.access_token);
    localStorage.setItem('tenant_id', result.tenant_id);
    localStorage.setItem('tenant_name', result.tenant_name);
    // ...
  }
};
```

### 5.2 API 请求适配

**文件：`frontend/src/config/api.ts`**

```typescript
// 登录响应类型更新
interface LoginResponse {
  access_token: string;
  token_type: string;
  role: string;
  tenant_id: number;
  tenant_name: string;
  is_super_admin: boolean;
  activity_types: Array<{ id: number; name: string; code: string | null }>;
}
```

### 5.3 Flutter App 改造

**文件：`event_app/lib/services/auth_service.dart`**

```dart
class AuthService {
  static const String _tokenKey = 'access_token';
  static const String _tenantIdKey = 'tenant_id';
  static const String _tenantNameKey = 'tenant_name';
  
  // 存储租户信息
  Future<void> saveTenantInfo(int tenantId, String tenantName) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setInt(_tenantIdKey, tenantId);
    await prefs.setString(_tenantNameKey, tenantName);
  }
  
  // 获取租户ID
  Future<int?> getTenantId() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getInt(_tenantIdKey);
  }
}
```

### 5.4 微信小程序改造

**文件：`miniprogram/utils/auth.js`**

```javascript
// 存储租户信息
function saveTenantInfo(data) {
  wx.setStorageSync('tenant_id', data.tenant_id);
  wx.setStorageSync('tenant_name', data.tenant_name);
}

// 获取租户ID
function getTenantId() {
  return wx.getStorageSync('tenant_id') || 1;
}

module.exports = {
  saveTenantInfo,
  getTenantId
};
```

---

## 六、迁移步骤

### 6.1 准备阶段

1. **备份数据库**
   ```bash
   mysqldump -u root -p sdm_db > backup_before_tenant.sql
   ```

2. **创建迁移分支**
   ```bash
   git checkout -b feature/multi-tenant
   ```

### 6.2 数据库迁移

```bash
# 1. 执行迁移脚本
mysql -u root -p sdm_db < migrations/add_tenant_fields.sql

# 2. 验证迁移结果
mysql -u root -p sdm_db -e "DESCRIBE activity;"
mysql -u root -p sdm_db -e "SELECT COUNT(*) FROM tenant;"
```

### 6.3 代码部署

1. **后端部署**
   - 更新所有 CRUD 文件
   - 更新 API 路由
   - 更新鉴权模块
   - 运行单元测试

2. **前端部署**
   - 更新登录页面
   - 更新 API 请求逻辑
   - 测试登录流程

3. **App 部署**
   - 更新 AuthService
   - 更新登录页面
   - 测试登录流程

### 6.4 测试验证

1. **创建测试租户**
   ```sql
   INSERT INTO tenant (name, code, status, plan) 
   VALUES ('测试租户', 'test', 1, 'basic');
   ```

2. **测试数据隔离**
   - 用不同租户账号登录
   - 验证只能看到本租户数据

---

## 七、回滚方案

### 7.1 数据库回滚

```sql
-- 删除租户字段
ALTER TABLE `activity` DROP COLUMN `tenant_id`;
ALTER TABLE `user` DROP COLUMN `tenant_id`;
ALTER TABLE `admin_user` DROP COLUMN `tenant_id`;
ALTER TABLE `activity_type` DROP COLUMN `tenant_id`;
ALTER TABLE `activity_participants` DROP COLUMN `tenant_id`;
ALTER TABLE `checkin_records` DROP COLUMN `tenant_id`;
ALTER TABLE `admin_activity_type_role` DROP COLUMN `tenant_id`;

-- 删除租户表
DROP TABLE IF EXISTS `tenant`;

-- 恢复原有唯一索引
ALTER TABLE `user` DROP INDEX `uk_tenant_phone`;
ALTER TABLE `user` ADD UNIQUE KEY `uk_user_phone` (`phone`);
-- ... 其他表类似
```


### 7.2 代码回滚

```bash
git checkout main
git branch -D feature/multi-tenant
```

---

## 八、工作量评估

| 阶段 | 工作内容 | 预估工时 |
|------|----------|----------|
| **数据库设计** | 表结构设计、索引优化 | 1 天 |
| **数据库迁移** | 编写迁移脚本、测试执行 | 1 天 |
| **后端改造** | 安全模块、鉴权、CRUD、API | 4-5 天 |
| **前端改造** | 登录页、API 适配 | 1 天 |
| **App 改造** | Flutter、小程序 | 1 天 |
| **测试验证** | 功能测试、数据隔离测试 | 2-3 天 |
| **文档编写** | API 文档、部署文档 | 1 天 |
| **总计** | - | **10-14 天** |

---

## 九、后续扩展

### 9.1 租户管理功能

- 租户列表、创建、编辑、禁用
- 租户配额管理（管理员数、活动数）
- 租户到期提醒

### 9.2 租户识别方式

- 子域名：`tenant1.yoursaas.com`
- 自定义域名绑定
- 请求头：`X-Tenant-Id`
  

*文档版本：v1.0*
*最后更新：2026-03-06*