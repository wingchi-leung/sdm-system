# SDM 系统 — 产品规格说明（Spec）

## 一、产品愿景与目标

**愿景**：打造一款 **All in One** 的平台型应用，承载平台的各类学习活动。

**核心目标**：
- 学习活动的全生命周期管理
- 平台人员与权限的清晰管理
- 支持多种登录方式与微信生态
- 预留社区能力与多租户扩展
- 数据私有化、部署灵活、UI 现代、可上架应用商店

## 二、功能规格与实现对照

### 2.1 学习活动的全生命周期管理

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **活动发布** | 管理员可创建并发布活动（名称、时间、标签等） | ✅ 已实现 | backend `POST /activities`；frontend 创建活动页；event_app / miniprogram 管理员发布活动 |
| **活动报名** | 用户可查看活动并提交报名信息（姓名、手机、证件号等） | ✅ 已实现 | backend `POST /participants`；C 端活动详情 → 报名页；支持已登录带 token、未登录直接填信息 |
| **活动签到** | 现场对已报名人员进行签到记录 | ✅ 已实现 | backend `POST /checkins`；frontend 签到页（选活动、参与人列表、多种签到方式） |
| **报名时支付** | 报名流程中可调用支付平台完成支付 | ❌ 未实现 | 需设计：支付渠道（微信支付等）、订单与报名关联、回调与状态同步 |

 

### 2.2 平台人员管理

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **用户注册** | 用户可通过手机号等完成注册 | ✅ 已实现 | backend `POST /users/register`；C 端注册页（姓名、手机、密码） |
| **不注册直接报名** | 未注册用户也可填写信息报名活动 | ✅ 已实现 | 报名接口支持不传 user token，仅填姓名、手机、证件号；写入 activity_participants，可选关联 user_id |
| **VIP 会员** | 会员身份与权益 | ❌ 未实现 | 需设计：会员等级/权益、与 user 表扩展或新表、报名/价格策略 |
| **黑名单** | 被拉黑用户报名时直接拦截 | ✅ 已实现 | user 表 isblock、block_reason；报名时按手机号查 user，若 isblock 则拒绝 |
| **管理员发布活动** | 管理员可创建活动 | ✅ 已实现 | 需 admin JWT；frontend / event_app / miniprogram 均有入口 |
| **管理员查看报名** | 查看某活动的报名人员及信息 | ✅ 已实现 | `GET /participants/{activity_id}/` 分页列表 |
| **管理员修改/删除活动** | 编辑活动信息、删除活动 | ✅ 已实现 | backend `PUT /activities/{id}`、`DELETE /activities/{id}`；frontend 活动列表页、编辑页；miniprogram 活动管理页、编辑页 |
| **活动人员查看** | 管理员可查看某活动的报名人员及信息 | ✅ 已实现 | `GET /participants/{activity_id}/` 分页列表（已加权限校验）；frontend 活动人员页；miniprogram 报名人员页 |
| **活动置为已结束** | 管理员可将活动标记为已结束 | ✅ 已实现 | `PUT /activities/{id}/status`，状态 3 为已结束 |
| **管理员分级与活动权限** | 超级管理员管全局；活动管理员按活动类型（如参、健康锻炼）授权，可创建/编辑/删除该类型下活动及查看报名等 | ❌ 未实现 | 见 [2.8 管理员分级与活动维度权限](#28-管理员分级与活动维度权限) |

**小结**：注册、不注册报名、黑名单、管理员发布/查报名/结束活动已实现；**VIP 会员**、**活动编辑/删除**、**管理员分级与活动权限**需在 spec 中明确并实现。

---

### 2.3 登录与注册

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **手机号 + 密码登录** | 用户使用手机号与密码登录 | ✅ 已实现 | `POST /auth/user-login`；C 端统一登录页 |
| **手机验证码登录** | 通过短信验证码登录/注册 | ❌ 未实现 | 需设计：短信通道、验证码生成与校验、与现有密码登录并存或替代 |
| **微信授权登录** | 支持微信一键登录/绑定 | ❌ 未实现 | 小程序可扩展 wx.login + 后端 session；App/Web 需微信开放平台 OAuth |
| **对接微信** | 与微信生态打通 | ⚠️ 部分 | 已有微信小程序端；登录方式与分享等可继续深化 |

**小结**：手机号+密码登录与注册已实现；**验证码登录**、**微信授权登录**为待实现规格。

---

### 2.4 社区能力（待详细设计）

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **角色：老师/学生** | 平台存在老师、学生两种角色 | ❌ 未实现 | 需在用户模型或权限中区分 |
| **老师发布学习视频** | 老师可创建“教导活动”，发布学习视频供跟练 | ❌ 未实现 | 需内容模型（视频、活动类型扩展）与发布流程 |
| **学生跟练与打卡** | 学生按个人情况跟练并上传打卡到 App | ❌ 未实现 | 需打卡内容类型、上传与展示、与“活动”或新实体的关联 |

**小结**：社区能力为**待详细设计**，本 spec 仅记录为远期规格，具体数据模型与接口需单独设计。

---

### 2.5 平台型能力（多租户）

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **切换团队/租户** | 类似飞书，支持多租户、切换当前团队 | ❌ 未实现 | 需设计：租户/组织模型、数据隔离、当前租户上下文、切换入口 |

**小结**：多租户为**待实现**的平台型能力，影响数据模型与鉴权，需单独迭代设计。

---

### 2.6 数据与部署

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **数据私有化** | 数据可本地/自建部署，不依赖公有 SaaS | ✅ 支持 | MySQL 自建；无强制外部依赖 |
| **本地部署** | 支持从个人笔记本等环境部署 | ✅ 支持 | 见《真机部署操作文档》；backend + MySQL + 前端/小程序配置 |
| **云服务器一键部署** | 支持在云上一键部署与迁移 |  部分 | 有部署文档；可补充 Docker/脚本实现“一键” |

**小结**：私有化与本地部署已满足；**一键云部署**可列为改进项。

---

### 2.7 美观与体验

| 规格项 | 描述 | 当前实现状态 | 说明/缺口 |
|--------|------|--------------|-----------|
| **现代 UI** | 参考 Teams、即刻、微信等现代风格 | 待优化 | 现有功能完整，视觉与交互可对照参考系做专项优化 |

**小结**：属非功能性规格，可在迭代中按设计规范逐步落地。

---

### 2.8 管理员分级与活动维度权限

#### 需求简述

- **超级管理员**：拥有系统全部管理权限（活动 CRUD、报名/签到管理、用户与黑名单、其他管理员维护等）。
- **活动管理员**：按**活动类型/大类**划分，仅能管理该类型下的所有活动（含创建、编辑、删除、查看报名与签到）。例如：
  - 「参」是一类活动，可能有多个具体活动实例（如每月几场）；参活动管理员可管理所有属于「参」类的活动的发布、编辑、删除、查看报名与签到，并可**创建**新的参类活动；
  - 「健康锻炼」是另一类，包含多场具体活动；健康锻炼管理员仅能管理健康锻炼大类下的全部活动，并可创建新的健康锻炼活动；
  - 参管理员不能操作健康锻炼类活动，健康锻炼管理员也不能操作参类活动，彼此按类型隔离。

授权粒度是**活动类型**（一类），不是单个活动实例：一名活动管理员可被授权多种类型；一种类型下可有多名活动管理员。

#### 实现思路

| 维度 | 说明 |
|------|------|
| **角色区分** | 在管理员身份上区分两类：超级管理员、活动管理员。推荐在 `admin_user` 表增加字段标识，例如 `is_super_admin`（1=超级管理员，0=活动管理员）。 |
| **活动类型与授权** | 引入**活动类型/大类**（如「参」「健康锻炼」）：每个具体活动实例归属某一类型。新增**管理员-活动类型关联表**（如 `admin_activity_type_role`），字段：`admin_user_id`、`activity_type_id`。表示该管理员被授权管理该**类型**下的所有活动；可为一管理员配置多条记录以绑定多种类型。 |
| **权限校验规则** | 先校验为管理员；再按操作区分：<br>• **全局类操作**（如：管理其他管理员、系统配置）：仅 `is_super_admin=1` 可执行。<br>• **创建活动**：超级管理员可创建任意类型活动；活动管理员只能选择其有权限的**活动类型**并创建该类型下的活动（创建时校验 `activity_type_id` 是否在其授权范围内）。<br>• **与某活动实例相关的操作**（编辑/删除/状态变更、查看该活动报名与签到）：`is_super_admin=1` 或 该活动所属的 `activity_type_id` 在管理员的 `admin_activity_type_role` 中 则可执行。 |
| **列表与可见范围** | 管理端「活动列表」：超级管理员看全部活动；活动管理员仅看到其有权限的**活动类型**下的全部活动实例。报名列表、签到等按「该活动是否属于其有权限的类型」过滤。 |

#### 数据模型（建议）

- **admin_user 表**：增加 `is_super_admin` tinyint，默认 0；1 表示超级管理员。
- **活动类型表**（如 `activity_type`，若当前无「类型」概念则新建）：  
  `id`、`type_name`（如「参」「健康锻炼」）、可选 `code` 等。用于区分活动大类，一个类型下对应多场具体活动。
- **activity 表**：增加 `activity_type_id` 外键关联 `activity_type.id`，表示该活动实例归属哪一大类。
- **admin_activity_type_role 表**（新建）：  
  `id`、`admin_user_id`、`activity_type_id`，唯一约束 `(admin_user_id, activity_type_id)`，外键关联 `admin_user.id` 与 `activity_type.id`。表示该管理员被授权管理该**类型**下的所有活动（含创建、编辑、删除、查看报名与签到）。

若后续需要更细粒度（如仅「查看报名」与「编辑活动」分离），可在该表增加权限位字段；首版可简化为「有记录即拥有该类型的全部管理权限」。

#### 接口与依赖

- 现有 `get_current_admin` 仍返回 `admin_user.id`；建议新增依赖如 `require_super_admin`（仅超级管理员通过）及 `require_activity_type_admin(activity_type_id)` / `require_activity_admin(activity_id)`（后者通过活动的 `activity_type_id` 判断：超级管理员或拥有该类型的活动管理员通过）。创建活动时：若为活动管理员，请求体须带 `activity_type_id` 且须在其授权类型内。
- 管理端：活动列表、报名/签到入口按当前管理员身份与「有权限的活动类型」过滤（只展示这些类型下的活动），避免越权操作。

#### 小结与状态

| 规格项 | 描述 | 当前实现状态 |
|--------|------|--------------|
| 超级管理员 | 拥有全部管理权限 | ❌ 未实现（当前所有管理员等价） |
| 活动类型（大类） | 活动归属类型，如「参」「健康锻炼」；一类下多场活动实例 | ❌ 未实现（当前 activity 无类型维度） |
| 活动管理员 | 按类型授权，可创建/编辑/删除/查看报名与签到（仅限其负责的类型下的活动） | ❌ 未实现 |
| 管理员-活动类型授权 | 为活动管理员绑定可管理的活动类型 | ❌ 未实现 |

实现时需：活动类型表与 activity 表增加 activity_type_id、admin_user 与 admin_activity_type_role 扩展、各管理接口按类型做权限校验、管理端仅展示有权限类型下的活动。

---

## 三、规格优先级与建议排期

### P0（已实现，需巩固）

- 活动 CRUD、状态流转、报名、签到
- 双角色鉴权（管理员 + 普通用户）、用户注册与登录
- 黑名单拦截、管理员查看报名与结束活动
- 管理端签到/创建活动/用户管理；C 端活动列表/详情/报名/我的

### P1（与痛点直接相关，建议优先）

0. **管理员分级与活动维度权限**：超级管理员拥有全部权限；活动管理员按**活动类型**授权（如参、健康锻炼等大类），可创建及管理该类型下全部活动实例，互不越权。实现要点见 [2.8 管理员分级与活动维度权限](#28-管理员分级与活动维度权限)。
1. **活动编辑与删除**：管理员可编辑活动信息、删除活动（接口 + 各端入口）；需与 2.8 权限规则结合（仅超级管理员或该活动管理员可操作）。
2. **活动人员查看** : 管理员可以查看活动的人员及信息
3. **支付**：报名环节接入支付（如微信支付），含订单与回调。
4. **登录方式扩展**：手机验证码登录/注册、微信授权登录（先小程序后 App/Web）。

### P2（平台化与体验）

4. **多租户**：租户/组织模型、数据隔离、切换团队。
5. **VIP 会员**：会员模型与基础权益。
6. **UI 规范**：对照 Teams/即刻/微信的视觉与交互规范优化。

### P3（扩展与文档）

7. **社区能力**：老师/学生角色、视频发布、跟练打卡（需先做详细设计）。
8. **一键云部署**：Docker/脚本与部署文档。
9. **上架文档**：安卓与 iOS 上架流程与成本说明。

---

## 四、非功能规格摘要

| 类型 | 说明 |
|------|------|
| **鉴权** | 管理员与普通用户双角色 JWT；接口按需使用 get_current_admin / get_current_user / get_current_user_optional；计划扩展管理员分级（超级管理员 vs 活动管理员），见 2.8 |
| **安全** | 生产环境强 JWT_SECRET、HTTPS、CORS 与敏感信息脱敏 |
| **数据** | MySQL 自建、表结构见 backend；支持私有化与迁移 |
| **多端** | 管理端 Web（React）、C 端 Flutter（Web/iOS/Android）、微信小程序，API 统一为 backend |

---

## 五、文档与变更

- **依据文档**：`痛点文档.md`、`工程现状与功能说明.md`
- **关联文档**：`backend/docs/鉴权说明.md`、各模块 README、真机部署操作文档
- **变更**：若痛点或工程现状有更新，应同步更新本 spec 与上述对照表。

---

*文档版本：v1 | 基于痛点文档与工程现状整理*
